[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  directory = "netlify/functions"

# Short cache TTL for data files — refreshed every 4 hours by GitHub Actions
[[headers]]
  for = "/data/*.json"
  [headers.values]
    Cache-Control = "public, max-age=300, s-maxage=300"
