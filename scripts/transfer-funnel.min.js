const TWF_MAX_SHOW=8,TWF_POS_NAMES=["","Goalkeepers","Defenders","Midfielders","Forwards"],TWF_POS_SHORT=["","GK","DEF","MID","FWD"];function twfDefaultFilters(){return{view:"quick",clubs:[],minutes:"any",form:"any",quality:"any",price:"any",own:"any",setPiece:"any",avail:"fit",defcon:!1,source:"all",search:"",sort:"xp"}}const TWF_QUICK_SHOW=6,TWF_QUICK_POOL=24;let twfViewPrefs={sort:"xp",view:"quick",filtersOpen:!1};function twfToggleFilterStrip(){twfViewPrefs.filtersOpen=!twfViewPrefs.filtersOpen,twfRerender()}const twfOpenCards=new Set;function twfToggleDetail(playerId,ev){ev&&ev.stopPropagation();const card=document.querySelector(`.twf-card[data-pid="${playerId}"]`);if(!card)return;const open=!card.classList.contains("is-open");card.classList.toggle("is-open",open),open?twfOpenCards.add(playerId):twfOpenCards.delete(playerId);const btn=card.querySelector(".twf-more");if(btn){btn.setAttribute("aria-expanded",String(open));const label=btn.querySelector(".twf-more-l");label&&(label.textContent=open?"Less":"All the numbers")}}function twfState(slotIdx){const i=slotIdx??transferState.activeSlot,slot=transferState.pending[i];return slot?(slot.funnel||(slot.funnel=twfSeedFilters(slot)),slot.funnel):(transferState.funnel||(transferState.funnel=twfDefaultFilters()),transferState.funnel)}function twfSeedFilters(slot){let prev=null;for(let i=transferState.pending.length-1;i>=0;i--){const other=transferState.pending[i];if(other!==slot&&other.funnel&&other.soldPlayer.position===slot.soldPlayer.position){prev=other.funnel;break}}const base=prev?Object.assign({},prev,{search:""}):twfDefaultFilters();return Object.assign(base,twfViewPrefs)}const TWF_HORIZON=5;function twfGWs(){if(typeof xpPlanGWs!="function")return[];const forced=typeof twStrategyHorizon=="function"?twStrategyHorizon():null;return xpPlanGWs(forced===1?1:TWF_HORIZON)}let _twfHistIndex=null,_twfHistSource=null;function twfHistory(playerId){return _twfHistSource!==playersDetailData&&(_twfHistIndex=new Map,(playersDetailData&&playersDetailData.players||[]).forEach(p=>_twfHistIndex.set(p.id,p.history||[])),_twfHistSource=playersDetailData),_twfHistIndex.get(playerId)||[]}let _twfFactCache=new Map;function twfFacts(p){const hit=_twfFactCache.get(p.id);if(hit)return hit;const raw=twfHistory(p.id),played=raw.filter(h=>h.minutes>0),last5=played.slice(-5),l5ppg=last5.length?last5.reduce((s,h)=>s+(h.total_points||0),0)/last5.length:p.ppg||0,seasonPpg=p.ppg||0,formRatio=seasonPpg>.5?l5ppg/seasonPpg:1,n=played.length,hauls=played.filter(h=>(h.total_points||0)>=8).length,blanks=played.filter(h=>(h.total_points||0)<=2).length,mins=typeof expectedMinutesModel=="function"?expectedMinutesModel(p):{pStart:0,expMins:0},per90=total=>p.minutes>0?total/p.minutes*90:0,facts={played:n,l5ppg,seasonPpg,formRatio,haulRate:n?hauls/n:0,blankRate:n?blanks/n:0,spark:raw.slice(-6).map(h=>({pts:h.total_points||0,mins:h.minutes||0,gw:h.round})),pStart:mins.pStart,expMins:mins.expMins,xGI90:p.xGI90!=null?p.xGI90:per90(p.xGI),xG90:p.xG90!=null?p.xG90:per90(p.xG),xA90:p.xA90!=null?p.xA90:per90(p.xA),xGC90:p.xGC90!=null?p.xGC90:per90(p.xGC),saves90:p.saves90!=null?p.saves90:per90(p.saves),defCon90:p.defCon90||0,savePct:p.saves+p.goalsConceded>0?p.saves/(p.saves+p.goalsConceded)*100:0,csPct:(p.starts||0)>0?p.cleanSheets/p.starts*100:0,bonusPerGame:n?played.reduce((s,h)=>s+(h.bonus||0),0)/n:0,netTransfers:(p.transfersIn||0)-(p.transfersOut||0)};return _twfFactCache.set(p.id,facts),facts}let _twfCohortCache={};function twfQualityMetric(p){return p.position<=2?p.minutes>0?p.xGC/p.minutes*90:0:p.minutes>0?p.xGI/p.minutes*90:0}function twfQualityLowerIsBetter(pos){return pos<=2}function twfQualityLabel(pos){return twfQualityLowerIsBetter(pos)?"xGC/90":"xGI/90"}function twfQualityPct(p){const pos=p.position;if(!_twfCohortCache[pos]){const floor=typeof minMinutesForCandidate=="function"?minMinutesForCandidate():0;_twfCohortCache[pos]=allPlayers.filter(x=>x.position===pos&&x.minutes>=floor).map(twfQualityMetric).sort((a,b)=>a-b)}const vals=_twfCohortCache[pos];if(!vals.length)return .5;const v=twfQualityMetric(p);let lo=0,hi=vals.length;for(;lo<hi;){const mid=lo+hi>>1;vals[mid]<=v?lo=mid+1:hi=mid}const pct=lo/vals.length;return twfQualityLowerIsBetter(p.position)?1-pct:pct}function twfInvalidate(){_twfFactCache=new Map,_twfCohortCache={}}function twfFixtureScore(f,pos){const ta=teamAnalysis[f.opponentId];let s;return ta?s=100-((pos<=2?f.isHome?ta.attackPowerAway!=null?ta.attackPowerAway:ta.attackPower:ta.attackPowerHome!=null?ta.attackPowerHome:ta.attackPower:f.isHome?ta.defensePowerAway!=null?ta.defensePowerAway:ta.defensePower:ta.defensePowerHome!=null?ta.defensePowerHome:ta.defensePower)||50):s=(5-(f.difficulty||3))/4*100,s+(f.isHome?6:-6)}function twfClubRun(teamId,pos,gws){const all=teamFixtures6[teamId]||[],perGW=gws.map(gw=>{const fixtures=all.filter(f=>f.event===gw),score=fixtures.reduce((s,f)=>s+twfFixtureScore(f,pos),0);return{gw,fixtures,score}}),flat=perGW.reduce((a,g)=>a.concat(g.fixtures),[]),runScore=perGW.length?perGW.reduce((s,g)=>s+g.score,0)/perGW.length:0,avgFdr=flat.length?flat.reduce((s,f)=>s+(f.difficulty||3),0)/flat.length:3,ta=teamAnalysis[teamId]||{},ss=typeof seasonStats<"u"&&seasonStats[teamId]||{},swing=typeof fixtureSwingData<"u"&&fixtureSwingData[teamId]||null;return{teamId,short:teams[teamId]&&teams[teamId].short_name||"?",name:teams[teamId]&&teams[teamId].name||"?",perGW,runScore,avgFdr,homes:flat.filter(f=>f.isHome).length,games:flat.length,blanks:perGW.filter(g=>!g.fixtures.length).length,doubles:perGW.filter(g=>g.fixtures.length>1).length,swing,form:ta.formRating!=null?ta.formRating:50,wins:ta.wins||0,draws:ta.draws||0,losses:ta.losses||0,attack:ta.attackPower!=null?ta.attackPower:50,defence:ta.defensePower!=null?ta.defensePower:50,attackHome:ta.attackPowerHome,attackAway:ta.attackPowerAway,defenceHome:ta.defensePowerHome,defenceAway:ta.defensePowerAway,xgTrend:ta.xgTrend||null,xgcTrend:ta.xgcTrend||null,xgDelta:ta.xgTrendDelta||0,xgcDelta:ta.xgcTrendDelta||0,avgGoals:ta.avgGoals||0,avgConceded:ta.avgConceded||0,csRate:ta.csRate||0,last5:ss.last5Form||[],csPercent:ss.csPercent,ftsPercent:ss.ftsPercent}}function twfAllClubRuns(pos,gws){return Object.keys(teams).map(k=>twfClubRun(parseInt(k,10),pos,gws)).sort((a,b)=>b.runScore-a.runScore)}function twfPasses(p,s,ctx){const f=twfFacts(p);if(s.avail==="fit"&&p.status!=="a"||s.minutes==="nailed"&&f.pStart<.8||s.form==="hot"&&!(f.formRatio>1.15&&f.played>=2)||s.quality==="top10"&&twfQualityPct(p)<.9||s.quality==="top25"&&twfQualityPct(p)<.75||s.price==="cheaper"&&!(p.price<ctx.outPrice-.05)||s.price==="same"&&Math.abs(p.price-ctx.outPrice)>.55||s.price==="upgrade"&&!(p.price>ctx.outPrice+.05)||s.own==="template"&&!(p.ownership>25)||s.own==="mid"&&!(p.ownership>=8&&p.ownership<=25)||s.own==="diff"&&!(p.ownership<8)||s.setPiece==="pens"&&p.penaltiesOrder!==1||s.setPiece==="sp"&&!(p.penaltiesOrder||p.cornersOrder||p.freekicksOrder))return!1;if(s.defcon){const threshold=p.position===2?10:12;if(p.position===1||f.defCon90<threshold)return!1}if(s.search){const q=s.search.toLowerCase();if(!p.name.toLowerCase().includes(q)&&!(p.fullName||"").toLowerCase().includes(q))return!1}return!0}function twfBasePool(slotIdx){const slot=transferState.pending[slotIdx],s=twfState(),pos=slot.soldPlayer.position,soldIds=new Set(transferState.pending.map(x=>x.soldPlayer.id)),boughtIds=new Set(transferState.pending.filter(x=>x.replacement).map(x=>x.replacement.id)),exclude=new Set(selectedPlayers.map(p=>p.id).concat([...boughtIds]));for(const id of soldIds)exclude.delete(id);exclude.add(slot.soldPlayer.id);const budget=twSlotBudget(slotIdx),floor=typeof minMinutesForCandidate=="function"?minMinutesForCandidate():0,shortlist=s.source==="favorites"?getTWShortlistIds():null;return allPlayers.filter(p=>p.position!==pos||exclude.has(p.id)?!1:shortlist?shortlist.has(p.id):!(p.price>budget+.001||p.minutes<floor||p.status!=="a"&&p.status!=="d"))}function twfEmptyReason(pos){const s=twfState();if(s.source==="favorites"){const starred=getTWShortlistIds();if(!starred.size)return"You have not starred any players yet \u2014 use the on the Players Analysis page and they show up here.";const shortlisted=allPlayers.filter(p=>starred.has(p.id)),owned=shortlisted.filter(p=>p.position===pos&&selectedPlayers.some(q=>q.id===p.id)),otherPos=shortlisted.filter(p=>p.position!==pos),why=twShortlistBreakdownText(owned,otherPos,TWF_POS_NAMES[pos]||"players");return`None of your ${shortlisted.length} starred players can fill this slot. ${why}`}return s.clubs.length?`No ${(TWF_POS_NAMES[pos]||"players").toLowerCase()} from the ${s.clubs.length} club${s.clubs.length===1?"":"s"} you selected pass your player filters.`:"Nothing in the market passes every filter."}function twfRerender(){const reopen=typeof v2MenuOpen<"u"?v2MenuOpen:null;typeof renderTWMarketPane=="function"&&renderTWMarketPane(),reopen&&typeof v2MenuReopen=="function"&&v2MenuReopen(reopen),typeof lucide<"u"&&lucide.createIcons()}function twfRenderMarket(el,slotIdx){twfInvalidate();const s=twfState(),slot=transferState.pending[slotIdx],sold=slot.soldPlayer,pos=sold.position,gws=twfGWs(),ctx={outPrice:sold.sellPrice||sold.price},base=twfBasePool(slotIdx),blocked=twBlockedClubIds(slotIdx);if(s.view==="quick"){el.innerHTML='<div class="twf">'+twfRenderHead(slotIdx,sold,gws,twfSortRowHTML())+twfRenderQuick(slot,base,gws,pos,slotIdx,blocked)+"</div>",typeof lucide<"u"&&lucide.createIcons(),s.search&&twfRestoreSearchFocus();return}const clubSet=new Set(s.clubs),survivors=(clubSet.size?base.filter(p=>clubSet.has(p.teamId)):base).filter(p=>twfPasses(p,s,ctx));el.innerHTML='<div class="twf">'+twfRenderHead(slotIdx,sold,gws,twfFilterBarHTML(base,survivors,ctx,pos,gws))+twfRenderCustom(slot,survivors,gws,pos,slotIdx,blocked,ctx)+"</div>",typeof lucide<"u"&&lucide.createIcons(),s.search&&twfRestoreSearchFocus()}function twfRenderHead(slotIdx,sold,gws,filterBar){const s=twfState(),views=`<div class="twf-views" role="tablist">
                <button class="twf-view${s.view==="quick"?" active":""}" role="tab" aria-selected="${s.view==="quick"}"
                    onclick="twfSetView('quick')"
                    data-tooltip="The best replacements we can find for ${escHTML(sold.name)}, ranked, with nothing to fill in.">${v2Icon("bolt")} Quick picks</button>
                <button class="twf-view${s.view==="custom"?" active":""}" role="tab" aria-selected="${s.view==="custom"}"
                    onclick="twfSetView('custom')"
                    data-tooltip="Narrow by fixture run, minutes, form, price, ownership and set pieces, then pick from what survives.">${v2Icon("tools")} Custom search</button>
            </div>`,switcher=typeof twSlotSwitcherHTML=="function"?twSlotSwitcherHTML():"",span=gws.length?`GW${gws[0]}\u2013GW${gws[gws.length-1]}`:"no upcoming gameweeks";return(typeof twStepHead=="function"?twStepHead({icon:"cart",title:"Replacements",hint:`Replacing ${escHTML(sold.name)}`}):"")+`<div class="twf-head">
                <div class="twf-head-top">
                    ${views}
                    ${filterBar||""}
                </div>
                
                ${switcher?`<div class="twf-head-bot">
                    ${switcher}
                    <span class="twf-span" data-tooltip="Every projection and every fixture run on this screen is judged over these gameweeks.">${escHTML(span)}</span>
                </div>`:""}
            </div>`}function twfPick(packed){const i=String(packed).indexOf(":");if(i<0)return;const key=packed.slice(0,i),value=packed.slice(i+1);if(key==="clubs"){twfClubPreset(value);return}twfSetFilter(key,value)}function twfActiveFilterCount(){const s=twfState(),d=twfDefaultFilters();return["quality","price","own","setPiece","minutes","form","avail","source"].filter(k=>s[k]!==d[k]).length+(s.defcon?1:0)+(s.clubs.length?1:0)}function twfFilterBarHTML(base,survivors,ctx,pos,gws){const s=twfState(),clubSet=new Set(s.clubs),scoped=clubSet.size?base.filter(p=>clubSet.has(p.teamId)):base,countIf=over=>{const merged=Object.assign({},s,over);return scoped.filter(p=>twfPasses(p,merged,ctx)).length},group=(label,key,options,tip)=>`
                <div class="apf-group">
                    <span class="v2-menu-label"${tip?` data-tooltip="${escHTML(tip)}"`:""}>${escHTML(label)}</span>
                    <div class="apf-controls">${options.map(o=>{const val=Object.prototype.hasOwnProperty.call(o,"cv")?o.cv:o.v,on=s[key]===val,n=countIf({[key]:val});return`<button class="filter-pill compact-pill${on?" active":""}"
                            onclick="twfPick('${key}:${o.v}')"
                            ${o.tip?`data-tooltip="${escHTML(o.tip)}"`:""}>${escHTML(o.l)}<em class="twf-n">${n}</em></button>`}).join("")}</div>
                </div>`,outPrice=ctx.outPrice,qLabel=twfQualityLabel(pos),active=twfActiveFilterCount(),groups=[group("Minutes","minutes",[{v:"any",l:"Any"},{v:"nailed",l:"Nailed",tip:"At least an 80% chance of starting."}],"The most common reason a transfer fails."),group("Form","form",[{v:"any",l:"Any"},{v:"hot",l:"Heating up",tip:"Scoring at least 15% above his season average over his last five."}],"The direction over his last five, not the level."),group("Underlying quality","quality",[{v:"any",l:"Any"},{v:"top25",l:"Top 25%"},{v:"top10",l:"Top 10%"}],`Percentile within his own position on ${qLabel}.`),group("Price","price",[{v:"any",l:"Any"},{v:"cheaper",l:"Cheaper",tip:`Under \xA3${outPrice.toFixed(1)}m \u2014 frees money for another slot.`},{v:"same",l:"Same money",tip:`Within \xA30.5m of \xA3${outPrice.toFixed(1)}m.`},{v:"upgrade",l:"Upgrade",tip:`Above \xA3${outPrice.toFixed(1)}m \u2014 spends into this slot.`}],`Against the \xA3${outPrice.toFixed(1)}m you get back.`),group("Ownership","own",[{v:"any",l:"Any"},{v:"template",l:"Template",tip:"Over 25% owned."},{v:"mid",l:"Mid",tip:"8\u201325% owned."},{v:"diff",l:"Differential",tip:"Under 8% owned."}],"Where he sits against the template."),group("Set pieces","setPiece",[{v:"any",l:"Any"},{v:"sp",l:"On any"},{v:"pens",l:"First-choice pens"}],"Taken from what FPL publishes, not inferred."),group("Availability","avail",[{v:"fit",l:"Fit only"},{v:"all",l:"Include doubts"}],"Fitness flags as published by FPL."),group("Shortlist","source",[{v:"all",l:"All players"},{v:"favorites",l:"Favourites",tip:"The players you starred on the Players Analysis page. Budget and minutes limits are lifted here, so an unaffordable target still shows, marked."}],"Everyone, or only the players you starred."),pos===1?"":group("Defensive contribution","defcon",[{v:"false",cv:!1,l:"Any"},{v:"true",cv:!0,l:"Clears the threshold"}],"Tackles, clearances, blocks, interceptions and recoveries. Worth 2 points once a per-match threshold is cleared \u2014 10 for defenders, 12 for midfielders.")].join(""),open=twfViewPrefs.filtersOpen,chosen=s.clubs.length?`<div class="twf-picked">${s.clubs.map(id=>`<button class="twf-picked-club" onclick="twfToggleClub(${id})"
                        data-tooltip="${escHTML(`Drop ${teams[id]&&teams[id].name||"this club"} from the selection.`)}">${escHTML(teams[id]&&teams[id].short_name||"?")}<i aria-hidden="true">\xD7</i></button>`).join("")}</div>`:"",clubPresets=[["all","All clubs",""],["swing","Improving swings","Fixtures get easier partway through"],["attack","Best attack","The eight strongest attacks"],["defence","Best defence","The eight strongest defences"]],clubGroup=`
                <div class="apf-group">
                    <span class="v2-menu-label" data-tooltip="Whose fixtures you want to own over ${escHTML(gws.length?`GW${gws[0]}\u2013GW${gws[gws.length-1]}`:"the gameweeks ahead")}.">Clubs</span>
                    <div class="apf-controls">${clubPresets.map(([v,l,note])=>`<button class="filter-pill compact-pill${v==="all"&&!s.clubs.length?" active":""}"
                            onclick="twfPick('clubs:${v}')"${note?` data-tooltip="${escHTML(note)}"`:""}>${escHTML(l)}</button>`).join("")}</div>
                    ${chosen}
                </div>`,bar=`<div class="twf-bar">
                <button class="apf-menu-btn twf-filters-btn${active?" is-on":""}${open?" is-open":""}"
                    onclick="twfToggleFilterStrip()" aria-expanded="${open}" aria-controls="twfFilterStrip"
                    data-tooltip="Clubs, minutes, form, quality, price, ownership, set pieces, availability and your shortlist.">
                    ${typeof v2Icon=="function"?v2Icon("sliders"):""}Filters${active?`<span class="apf-menu-n">${active}</span>`:""}
                    <span class="twf-filters-caret" aria-hidden="true"></span>
                </button>
                
                <div class="twf-search-wrap">
                    ${typeof v2Icon=="function"?v2Icon("eye"):""}
                    <input class="twf-search" type="text" placeholder="Search by name\u2026" value="${escHTML(s.search)}" oninput="twfSearch(this.value)">
                </div>
            </div>`;if(!open)return bar;const strip=`<div class="twf-filters-strip" id="twfFilterStrip">
                <div class="twf-panel-head">
                    <span class="twf-panel-h">${survivors.length?`${survivors.length} player${survivors.length===1?"":"s"} match`:"Nothing matches"}</span>
                    ${active?'<button class="twf-panel-reset" onclick="twfResetFilters()">Clear all</button>':""}
                </div>
                ${survivors.length?"":'<div class="twf-panel-dead">Every count below reads 0 because removing any single filter still leaves nothing \u2014 more than one is doing the cutting.</div>'}
                <div class="twf-filters-strip-groups">${clubGroup}${groups}</div>
            </div>`;return bar+strip}function twfRenderFooter(n,pos){return n!==0?"":`<div class="twf-foot">
                <span class="twf-foot-why">${escHTML(twfEmptyReason(pos))}</span>
            </div>`}function twfProjection(p,gws){const all=teamFixtures6[p.teamId]||[],comp={appearance:0,attack:0,cleanSheet:0,saves:0,bonus:0,defCon:0,conceded:0,cards:0},perGW=gws.map(gw=>{const matches=all.filter(f=>f.event===gw),immediate=all.length>0&&all[0].event===gw;let sum=0;return matches.forEach(f=>{const d=projectPlayerPointsDetailed(p,{fixture:f,applyEpFloor:immediate});Object.keys(comp).forEach(k=>{comp[k]+=d[k]||0}),sum+=d.total}),{gw,xp:Math.round(sum*10)/10,matches}});return{total:Math.round(perGW.reduce((s,g)=>s+g.xp,0)*10)/10,perGW,comp}}const TWF_SORTS=[{v:"xp",l:"Best projection",tip:"Projected points over the horizon above."},{v:"value",l:"Value",tip:"Projected points per \xA3m."},{v:"form",l:"Form",tip:"Points per game over his last five appearances."},{v:"diff",l:"Differential",tip:"Lowest ownership first."},{v:"minutes",l:"Safest minutes",tip:"Highest chance of starting first."}];function twfSortRowHTML(){const s=twfState();return`<div class="twf-bar is-inline">
                ${v2MenuHTML({key:"twf-sort",icon:"sliders",label:"Sort",value:s.sort,onPick:"twfSetSort",options:TWF_SORTS.map(o=>({value:o.v,label:o.l,note:o.tip}))})}
            </div>`}function twfApplySort(scored,sort){const facts=x=>twfFacts(x.p);return sort==="value"?scored.sort((a,b)=>b.proj.total/b.p.price-a.proj.total/a.p.price):sort==="form"?scored.sort((a,b)=>facts(b).l5ppg-facts(a).l5ppg):sort==="diff"?scored.sort((a,b)=>a.p.ownership-b.p.ownership):sort==="minutes"?scored.sort((a,b)=>facts(b).pStart-facts(a).pStart):scored.sort((a,b)=>b.proj.total-a.proj.total),scored}function twfRenderQuick(slot,base,gws,pos,slotIdx,blocked){const sold=slot.soldPlayer,soldProj=twfProjection(sold,gws),budget=twSlotBudget(slotIdx),s=twfState(),eligible=base.filter(p=>p.status==="a"&&!blocked.has(p.teamId)),scored=eligible.map(p=>{const proj=twfProjection(p,gws);return{p,proj,gain:Math.round((proj.total-soldProj.total)*10)/10}}).sort((a,b)=>b.proj.total-a.proj.total),shown=twfApplySort(scored.slice(0,TWF_QUICK_POOL),s.sort).slice(0,TWF_QUICK_SHOW),setAside=base.length-eligible.length;if(!shown.length)return`<div class="twf-body quick">
                        <div class="twf-empty">${escHTML(twfEmptyReason(pos))}
                            <div><button class="twf-linkbtn" onclick="twfSetView('custom')">Open the custom search</button> to see everyone, with the reason each one is out.</div>
                        </div>
                    </div>
                    `;const cards=shown.map((x,i)=>twfCardHTML(x,i,sold,soldProj,gws,pos,budget,!1)).join(""),bestGain=scored[0].gain,noUpgrade=bestGain<=0?`<div class="twf-noupgrade">Nothing you can afford projects better than <strong>${escHTML(sold.name)}</strong> over these gameweeks \u2014 the closest is ${bestGain===0?"level with him":`${Math.abs(bestGain).toFixed(1)} behind`}. This is a transfer worth not making unless you need the money elsewhere.</div>`:"",countLine=shown.length<scored.length?s.sort==="xp"?`Showing the best ${shown.length} of ${scored.length}`:`Showing ${shown.length} of the ${Math.min(TWF_QUICK_POOL,scored.length)} strongest, by ${TWF_SORTS.find(o=>o.v===s.sort).l.toLowerCase()}`:`${scored.length} player${scored.length===1?"":"s"} you can afford`,countTip=`Everyone here is affordable against ${sold.name}.`+(setAside>0?` ${setAside} more ${setAside===1?"is":"are"} set aside as injured, doubtful, or a fourth from a club you already have three of.`:"");return`<div class="twf-body quick">
                <div class="twf-section-head">
                    <span class="twf-section-sub" data-tooltip="${escHTML(countTip)}">${escHTML(countLine)}<span class="twf-span" data-tooltip="Every projection and every fixture run below is judged over these gameweeks."> \xB7 ${escHTML(gws.length?`GW${gws[0]}\u2013GW${gws[gws.length-1]}`:"no upcoming gameweeks")}</span></span>
                </div>
                <div class="twf-out" data-tooltip="Every card below is priced against this player over the same gameweeks.">
                    <span class="twf-out-l">Selling</span>
                    <span class="twf-out-name">${escHTML(sold.name)}</span>
                    <span class="twf-out-sub">${escHTML(sold.team)} \xB7 \xA3${(sold.sellPrice||sold.price).toFixed(1)}m</span>
                    <span class="twf-out-xp">${soldProj.total.toFixed(1)}<i>xP</i></span>
                </div>
                ${noUpgrade}
                <div class="twf-cards">${cards}</div>
            </div>
            `}function twfRenderCustom(slot,survivors,gws,pos,slotIdx,blocked,ctx){const sold=slot.soldPlayer,soldProj=twfProjection(sold,gws),budget=twSlotBudget(slotIdx),scored=survivors.map(p=>{const proj=twfProjection(p,gws);return{p,proj,gain:Math.round((proj.total-soldProj.total)*10)/10}}).sort((a,b)=>b.proj.total-a.proj.total),shown=scored.slice(0,8),cards=shown.length?shown.map((x,i)=>twfCardHTML(x,i,sold,soldProj,gws,pos,budget,blocked.has(x.p.teamId))).join(""):`<div class="twf-empty">${escHTML(twfEmptyReason(pos))}
                    <div><button class="twf-linkbtn" onclick="twfResetFilters()">Reset the filters</button> or <button class="twf-linkbtn" onclick="twfSetView('quick')">see the quick picks</button>.</div></div>`;return`<div class="twf-body custom">
                <div class="twf-results">
                    
                    <div class="twf-section-head">
                        <span class="twf-section-sub">${shown.length<scored.length?`Showing the best ${shown.length} of ${scored.length}`:`${scored.length} player${scored.length===1?"":"s"} match${scored.length===1?"es":""}`}<span class="twf-span" data-tooltip="Every projection and every fixture run below is judged over these gameweeks."> \xB7 ${escHTML(gws.length?`GW${gws[0]}\u2013GW${gws[gws.length-1]}`:"no upcoming gameweeks")}</span></span>
                    </div>
                    <div class="twf-out" data-tooltip="Every card below is priced against this player over the same gameweeks.">
                        <span class="twf-out-l">Selling</span>
                        <span class="twf-out-name">${escHTML(sold.name)}</span>
                        <span class="twf-out-sub">${escHTML(sold.team)} \xB7 \xA3${(sold.sellPrice||sold.price).toFixed(1)}m</span>
                        <span class="twf-out-xp">${soldProj.total.toFixed(1)}<i>xP</i></span>
                    </div>
                    <div class="twf-cards">${cards}</div>
                </div>
            </div>
            ${twfRenderFooter(scored.length,pos)}`}function twfCardHTML(x,i,sold,soldProj,gws,pos,budget,clubFull){const p=x.p,proj=x.proj,f=twfFacts(p),gain=x.gain,cls=gain>.3?"up":gain<-.3?"down":"flat",unafford=p.price>budget+.001,risk=(proj.comp.conceded||0)+(proj.comp.cards||0),strip=proj.perGW.map(g=>{if(!g.matches.length)return`<span class="twf-gw blank" data-tooltip="GW${g.gw} \u2014 blank, no fixture">GW${g.gw}<b>\u2014</b></span>`;const labels=g.matches.map(m=>`${(m.opponent||"?").slice(0,3)}${m.isHome?" (H)":" (A)"}`).join(" + ");return`<span class="twf-gw fdr-${Math.round(g.matches.reduce((s2,m)=>s2+(m.difficulty||3),0)/g.matches.length)}" data-tooltip="${escHTML(`GW${g.gw} \u2014 ${labels}. Projected ${g.xp.toFixed(1)} points.`)}">GW${g.gw}<b>${g.xp.toFixed(1)}</b></span>`}).join(""),peak=Math.max(6,...f.spark.map(z=>z.pts)),spark=f.spark.length?f.spark.map(z=>`<i class="twf-sp${z.mins===0?" out":z.pts>=8?" haul":""}" style="height:${Math.max(8,z.pts/peak*100)}%"
                    data-tooltip="${escHTML(`GW${z.gw}: ${z.pts} point${z.pts===1?"":"s"}${z.mins===0?", did not play":`, ${z.mins} minutes`}`)}"></i>`).join(""):'<i class="twf-sp none"></i>',posStats=twfPositionStats(p,f,pos),sp=[p.penaltiesOrder===1?'<span class="twf-sp-badge pen" data-tooltip="First-choice penalty taker.">PEN</span>':"",p.penaltiesOrder>1?`<span class="twf-sp-badge" data-tooltip="Number ${p.penaltiesOrder} on penalties.">PEN${p.penaltiesOrder}</span>`:"",p.cornersOrder===1?'<span class="twf-sp-badge" data-tooltip="First-choice corner taker.">COR</span>':"",p.freekicksOrder===1?'<span class="twf-sp-badge" data-tooltip="First-choice direct free-kick taker.">FK</span>':""].join(""),flag=p.status==="d"?`<span class="twf-flag doubt" data-tooltip="${escHTML(p.news||"Fitness doubt")}${p.chanceNextRound!=null?escHTML(` \u2014 ${p.chanceNextRound}% chance of playing`):""}">?</span>`:p.status!=="a"?'<span class="twf-flag out" data-tooltip="Unavailable">OUT</span>':"",run=twfClubRun(p.teamId,pos,gws),swingChip=run.swing?`<span class="twf-swing ${run.swing.direction}" data-tooltip="${escHTML(`Average difficulty ${run.swing.currentFdr} over the next three, then ${run.swing.futureFdr} from GW${run.swing.swingGW}.`)}">${run.swing.direction==="improving"?"\u2191 Eases":"\u2193 Hardens"} GW${run.swing.swingGW}</span>`:"",trend=f.formRatio>1.15?'<span class="twf-dir up">\u25B2 heating up</span>':f.formRatio<.85?'<span class="twf-dir down">\u25BC cooling</span>':'<span class="twf-dir flat">steady</span>',hero=typeof v2PlayerHeroHTML=="function"?v2PlayerHeroHTML(p,{size:"compact",chip:`#${i+1}`,chipClass:gain>.3?"is-up":gain<-.3?"is-down":""}):"",topBits=[flag,sp,clubFull?`<span class="twf-block" data-tooltip="You already hold three players from ${escHTML(p.team)}.">CLUB FULL</span>`:"",unafford&&!clubFull?`<span class="twf-block" data-tooltip="\xA3${p.price.toFixed(1)}m is outside the \xA3${budget.toFixed(1)}m this slot has.">OVER BUDGET</span>`:"",typeof priceChangeBadge=="function"?priceChangeBadge(p):""].filter(Boolean).join(""),isOpen=twfOpenCards.has(p.id);return`<div class="twf-card${unafford||clubFull?" unafford":""}${isOpen?" is-open":""}" data-pid="${p.id}" onclick="twPreviewPlayer(${p.id})">
                <div class="twf-hero">${hero}</div>

                <div class="twf-verdict">
                    ${topBits?`<span class="twf-card-top">${topBits}</span>`:""}
                    <span class="twf-xp" data-tooltip="${escHTML(`Projected ${proj.total.toFixed(1)} points over GW${gws[0]}\u2013GW${gws[gws.length-1]}, against ${soldProj.total.toFixed(1)} for ${sold.name}.`)}">${proj.total.toFixed(1)}<i>xP</i></span>
                    <span class="twf-gain ${cls}">${gain>0?"+":""}${gain.toFixed(1)}<i>vs ${escHTML(sold.name)}</i></span>
                    
                </div>

                <div class="twf-strip" data-tooltip="Projected points gameweek by gameweek \u2014 team quality, opponent, venue, blanks and doubles are all already in these numbers.">${strip}</div>

                <button class="twf-more" onclick="twfToggleDetail(${p.id}, event)" aria-expanded="${isOpen}"
                    data-tooltip="Form, minutes, hauls, set pieces and the club's own numbers.">
                    <span class="twf-more-l">${isOpen?"Less":"All the numbers"}</span>
                    <span class="twf-more-c" aria-hidden="true">${v2Icon("down")}</span>
                </button>

                <div class="twf-cols">
                    <div class="twf-col">
                        <div class="twf-col-h">Player form</div>
                        <div class="twf-spark">${spark}</div>
                        <div class="twf-kv"><span>Last 5 / season</span><b>${f.l5ppg.toFixed(1)} / ${f.seasonPpg.toFixed(1)}</b>${trend}</div>
                        <div class="twf-kv" data-tooltip="Chance of starting, and the minutes he averages when he does."><span>Starts</span><b>${Math.round(f.pStart*100)}%</b><span class="twf-dim">${Math.round(f.expMins)}\u2032</span></div>
                        <div class="twf-kv" data-tooltip="Share of his appearances returning 8 or more points, against the share returning 2 or fewer. Ceiling and floor."><span>Hauls / blanks</span><b>${Math.round(f.haulRate*100)}%</b><span class="twf-dim">${Math.round(f.blankRate*100)}%</span></div>
                        <div class="twf-kv"><span>Bonus / game</span><b>${f.bonusPerGame.toFixed(1)}</b></div>
                        ${risk<-.15?`<div class="twf-kv" data-tooltip="Goals conceded and cards, netted off the projection above."><span>Conceded &amp; cards</span><b class="down">${risk.toFixed(1)}</b></div>`:""}
                        <div class="twf-kv" data-tooltip="Selected by ${p.ownership.toFixed(1)}% of managers${f.netTransfers?`, ${f.netTransfers>0?"in":"out"} ${Math.abs(f.netTransfers).toLocaleString()} net this gameweek`:""}."><span>Owned</span><b>${p.ownership.toFixed(1)}%</b>${f.netTransfers?`<span class="twf-dim ${f.netTransfers>0?"up":"down"}">${f.netTransfers>0?"\u2191":"\u2193"}${Math.abs(f.netTransfers)>=1e3?Math.round(Math.abs(f.netTransfers)/1e3)+"k":Math.abs(f.netTransfers)}</span>`:""}</div>
                        ${posStats}
                    </div>
                    <div class="twf-col">
                        <div class="twf-col-h">${escHTML(run.name)}</div>
                        <div class="twf-kv" data-tooltip="Attack and defence power, 0\u2013100, blending goals scored and conceded with FPL's own strength ratings."><span>Attack / defence</span><b>${run.attack}</b><span class="twf-dim">${run.defence}</span></div>
                        <div class="twf-kv" data-tooltip="Recency-weighted form over the last five: W${run.wins} D${run.draws} L${run.losses}."><span>Team form</span><b>${run.form}</b><span class="twf-form5">${(run.last5||[]).slice(-5).map(z=>`<i class="twf-r ${z.toLowerCase()}">${z}</i>`).join("")}</span></div>
                        <div class="twf-kv"><span>Goals for / against</span><b>${run.avgGoals.toFixed(1)}</b><span class="twf-dim">${run.avgConceded.toFixed(1)}</span></div>
                        <div class="twf-kv" data-tooltip="Share of matches with a clean sheet, and with no goal scored."><span>Clean sheets / blanks</span><b>${run.csPercent!=null?run.csPercent+"%":Math.round(run.csRate*100)+"%"}</b>${run.ftsPercent!=null?`<span class="twf-dim">${run.ftsPercent}%</span>`:""}</div>
                        
                        <div class="twf-kv" data-tooltip="Team expected goals and expected goals conceded per game, against their own season averages. Needs about ten matches before a six-match window says anything."><span>xG / xGC trend</span>${run.xgTrend||run.xgcTrend?`<b class="${run.xgTrend==="rising"?"up":run.xgTrend==="falling"?"down":""}">${escHTML(run.xgTrend||"\u2014")}</b><span class="twf-dim ${run.xgcTrend==="improving"?"up":run.xgcTrend==="worsening"?"down":""}">${escHTML(run.xgcTrend||"\u2014")}</span>`:'<b class="twf-dim">not enough games yet</b>'}</div>
                        <div class="twf-kv" data-tooltip="${escHTML(`${run.games} match${run.games===1?"":"es"} across ${gws.length} gameweeks, ${run.homes} at home. Average difficulty ${run.avgFdr.toFixed(1)}.`)}"><span>Run</span><b>${run.games} in ${gws.length}</b><span class="twf-dim">${run.homes}H \xB7 FDR ${run.avgFdr.toFixed(1)}</span></div>
                        ${swingChip?`<div class="twf-kv"><span>Swing</span>${swingChip}</div>`:""}
                    </div>
                </div>
            </div>`}function twfPositionStats(p,f,pos){const pct=Math.round(twfQualityPct(p)*100),rows=[],kv=(label,value,tip)=>`<div class="twf-kv" ${tip?`data-tooltip="${escHTML(tip)}"`:""}><span>${escHTML(label)}</span><b>${escHTML(value)}</b></div>`;return pos===1?(rows.push(kv("Save %",f.savePct.toFixed(0)+"%",`Saves as a share of shots on target faced. ${pct}th percentile among goalkeepers.`)),rows.push(kv("Saves / 90",f.saves90.toFixed(1),"Three saves is one point.")),rows.push(kv("xGC / 90",f.xGC90.toFixed(2),"Expected goals conceded per 90 \u2014 the shot quality he faces.")),rows.push(kv("Clean sheets",f.csPct.toFixed(0)+"%","Share of his starts ending in a clean sheet."))):pos===2?(rows.push(kv("xGC / 90",f.xGC90.toFixed(2),"Expected goals conceded per 90 while he is on the pitch.")),rows.push(kv("Clean sheets",f.csPct.toFixed(0)+"%","Share of his starts ending in a clean sheet \u2014 four points each.")),rows.push(kv("xGI / 90",f.xGI90.toFixed(2),`Expected goal involvements per 90 \u2014 where a defender separates from the rest. ${pct}th percentile among defenders.`)),rows.push(kv("Def. contribution / 90",f.defCon90.toFixed(1),"Tackles, clearances, blocks, interceptions and recoveries. Ten in a match is worth 2 points."))):pos===3?(rows.push(kv("xGI / 90",f.xGI90.toFixed(2),`Expected goal involvements per 90. ${pct}th percentile among midfielders.`)),rows.push(kv("xG / 90",f.xG90.toFixed(2),"Expected goals per 90.")),rows.push(kv("xA / 90",f.xA90.toFixed(2),"Expected assists per 90.")),rows.push(kv("Def. contribution / 90",f.defCon90.toFixed(1),"Twelve in a match is worth 2 points for a midfielder."))):(rows.push(kv("xG / 90",f.xG90.toFixed(2),`Expected goals per 90. ${pct}th percentile among forwards.`)),rows.push(kv("xGI / 90",f.xGI90.toFixed(2),"Expected goal involvements per 90.")),rows.push(kv("Goals vs xG",`${p.goals} / ${p.xG.toFixed(1)}`,p.goals-p.xG>1?"Scoring above his expected goals \u2014 some of that is finishing, some of it regresses.":p.goals-p.xG<-1?"Scoring below his expected goals \u2014 the chances are there.":"Scoring roughly in line with his chances."))),rows.join("")}function twfSetView(view){const s=twfState();s.view=view,twfViewPrefs.view=view,twfRerender()}function twfToggleClub(teamId){const s=twfState(),i=s.clubs.indexOf(teamId);i>=0?s.clubs.splice(i,1):s.clubs.push(teamId),twfRerender()}function twfClubPreset(kind){const s=twfState(),slot=transferState.pending[transferState.activeSlot];if(!slot)return;const pos=slot.soldPlayer.position,runs=twfAllClubRuns(pos,twfGWs());kind==="all"?s.clubs=[]:kind==="swing"?s.clubs=runs.filter(r=>r.swing&&r.swing.direction==="improving").map(r=>r.teamId):kind==="attack"?s.clubs=[...runs].sort((a,b)=>b.attack-a.attack).slice(0,8).map(r=>r.teamId):kind==="defence"&&(s.clubs=[...runs].sort((a,b)=>b.defence-a.defence).slice(0,8).map(r=>r.teamId)),twfRerender()}function twfSetFilter(key,value){const s=twfState();if(key==="defcon"){s.defcon=value===!0||value==="true",twfRerender();return}s[key]=s[key]===value&&value!=="any"&&key!=="source"&&key!=="avail"?"any":value,twfRerender()}function twfToggleDefcon(){const s=twfState();s.defcon=!s.defcon,twfRerender()}function twfSetSort(sort){twfState().sort=sort,twfViewPrefs.sort=sort,twfRerender()}function twfResetFilters(){const s=twfState(),keep={sort:s.sort,view:s.view};Object.assign(s,twfDefaultFilters(),keep),twfRerender()}let _twfSearchTimer=null;function twfSearch(value){twfState().search=value,clearTimeout(_twfSearchTimer),_twfSearchTimer=setTimeout(twfRerender,220)}function twfRestoreSearchFocus(){const inp=document.querySelector(".twf-search");!inp||document.activeElement===inp||(inp.focus(),inp.selectionStart=inp.selectionEnd=inp.value.length)}
