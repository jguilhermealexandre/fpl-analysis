let sdArticles=[],sdArchived=!1,sdBoot=null,sdFixtures=null,sdHistory=null;const sdNum=v=>parseFloat(v)||0,sdRound=(v,n=2)=>Math.round(v*Math.pow(10,n))/Math.pow(10,n);function sdTeamName(id){const t=(sdBoot?.teams||[]).find(x=>x.id===id);return t?t.short_name:"???"}function sdPlayers(){return(sdBoot?.elements||[]).map(e=>({id:e.id,name:e.web_name,team:sdTeamName(e.team),teamId:e.team,pos:["","GK","DEF","MID","FWD"][e.element_type],position:e.element_type,price:e.now_cost/10,points:e.total_points,minutes:e.minutes,goals:e.goals_scored,assists:e.assists,bonus:e.bonus,xG:sdNum(e.expected_goals),xA:sdNum(e.expected_assists),xGI:sdNum(e.expected_goal_involvements),xGC:sdNum(e.expected_goals_conceded),cs:e.clean_sheets,own:sdNum(e.selected_by_percent),form:sdNum(e.form),ppg:sdNum(e.points_per_game),status:e.status,news:e.news,costChange:e.cost_change_event/10,tIn:e.transfers_in_event,tOut:e.transfers_out_event,starts:e.starts,threat:sdNum(e.threat),creativity:sdNum(e.creativity),ictIndex:sdNum(e.ict_index),code:e.code}))}function sdSubject(player,stat){return!player||!player.name?null:{name:player.name,code:player.code==null?null:player.code,stat:stat||""}}function sdRoundsPlayed(){const rounds=new Set;return(sdHistory?.players||[]).forEach(p=>(p.history||[]).forEach(h=>{(h.minutes||0)>0&&rounds.add(h.round)})),rounds.size}function sdLastRound(){let last=0;return(sdHistory?.players||[]).forEach(p=>(p.history||[]).forEach(h=>{h.round>last&&(last=h.round)})),last}function sdCompletedRound(){let last=0;if((sdBoot?.events||[]).forEach(e=>{e.finished&&e.data_checked&&e.id>last&&(last=e.id)}),last)return last;const byRound={};return(sdFixtures||[]).forEach(f=>{if(f.event==null)return;const r=byRound[f.event]||(byRound[f.event]={total:0,done:0});r.total++,f.finished&&r.done++}),Object.keys(byRound).forEach(k=>{const r=byRound[k];r.total>0&&r.done===r.total&&Number(k)>last&&(last=Number(k))}),last}function sdRoundRows(round){const byId={};sdPlayers().forEach(p=>{byId[p.id]=p});const rows=[];return(sdHistory?.players||[]).forEach(p=>{(p.history||[]).forEach(h=>{if(h.round!==round)return;const base=byId[p.id];base&&rows.push({...base,gwPoints:h.total_points,gwMinutes:h.minutes,gwGoals:h.goals_scored,gwAssists:h.assists,gwBonus:h.bonus,gwXG:sdNum(h.expected_goals),gwXA:sdNum(h.expected_assists),gwXGI:sdNum(h.expected_goal_involvements),gwCS:h.clean_sheets,gwOpponent:sdTeamName(h.opponent_team),gwHome:h.was_home,gwBps:h.bps})})}),rows}function sdWordCount(markdown){return String(markdown).replace(/^:::.*$/gm," ").replace(/\{fdr:[\d.]+\}/g," ").replace(/[|#*>`_-]/g," ").split(/\s+/).filter(Boolean).length}function sdReadTime(markdown){return Math.max(1,Math.ceil(sdWordCount(markdown)/200))}function sdTable(headers,rows){return"| "+headers.join(" | ")+` |
|`+headers.map(()=>"---").join("|")+`|
`+rows.map(r=>"| "+r.join(" | ")+" |").join(`
`)+`
`}const SD_CALLOUT_LABEL={insight:"Scout's key takeaway",warning:"Risk check",data:"What the data says",tactical:"Tactical read"};function sdCallout(variant,body){return`:::callout ${SD_CALLOUT_LABEL[variant]?variant:"insight"}
${body.trim()}
:::

`}function sdActions(items){const rows=items.filter(Boolean).map(a=>`${a.verdict} | ${a.subject} | ${a.reason}`).join(`
`);return rows?`:::actions
${rows}
:::

`:""}function sdTakeaways(points){const rows=points.filter(Boolean).map(t=>`- ${t}`).join(`
`);return rows?`:::takeaways
${rows}
:::

`:""}function sdFixtureStrip(items){const rows=items.filter(Boolean).map(f=>`${f.gw} | ${f.opponent} | ${f.difficulty}`).join(`
`);return rows?`:::fixtures
${rows}
:::

`:""}function sdFdrCell(value){return`{fdr:${sdRound(value,1)}}`}const SD_CHIP_NAME={bboost:"Bench Boost","3xc":"Triple Captain",freehit:"Free Hit",wildcard:"Wildcard",manager:"Assistant Manager"};function sdEvent(gw){return(sdBoot?.events||[]).find(e=>e.id===gw)||null}function sdNextGw(){const next=(sdBoot?.events||[]).find(e=>e.is_next);return next?next.id:sdLastRound()+1}function sdPlayerById(id){return sdPlayers().find(p=>p.id===id)||null}function sdTeam(id){return(sdBoot?.teams||[]).find(t=>t.id===id)||null}function sdStrength(teamId,side,home){const t=sdTeam(teamId);return t?side==="attack"?home?t.strength_attack_home:t.strength_attack_away:home?t.strength_defence_home:t.strength_defence_away:null}function sdStrengthWord(value,all){if(value==null||!all.length)return"average";const sorted=[...all].sort((a,b)=>a-b),rank=sorted.filter(v=>v<value).length/sorted.length;return rank>=.8?"one of the strongest in the division":rank>=.6?"above average":rank<=.2?"one of the weakest in the division":rank<=.4?"below average":"around the divisional average"}function sdTeamFixtures(teamId,gws){return(sdFixtures||[]).filter(f=>gws.includes(f.event)&&(f.team_h===teamId||f.team_a===teamId)).map(f=>{const home=f.team_h===teamId;return{gw:f.event,home,opponent:sdTeamName(home?f.team_a:f.team_h),difficulty:home?f.team_h_difficulty:f.team_a_difficulty}}).sort((a,b)=>a.gw-b.gw)}function sdUpcomingGws(count){const from=sdNextGw();return[...new Set((sdFixtures||[]).map(f=>f.event).filter(g=>g&&g>=from))].sort((a,b)=>a-b).slice(0,count)}function sdGenGameweekDebrief(){const gw=sdCompletedRound();if(!gw)return null;const rows=sdRoundRows(gw);if(!rows.length)return null;const ev=sdEvent(gw),byPoints=[...rows].sort((a,b)=>b.gwPoints-a.gwPoints),hero=byPoints[0],top=byPoints.slice(0,12),officialTop=ev&&ev.top_element_info?ev.top_element_info:null,heroIsTop=!officialTop||hero.gwPoints>=officialTop.points,heroLine=hero.gwGoals>0||hero.gwAssists>0?`${hero.gwGoals} goal${hero.gwGoals===1?"":"s"}${hero.gwAssists?` and ${hero.gwAssists} assist${hero.gwAssists===1?"":"s"}`:""}`:`${hero.gwPoints} points without a goal or assist`;let md=`## The headline

`;md+=heroIsTop?`**${hero.name}** was the story of Gameweek ${gw}: `:`**${hero.name}** led the players tracked here in Gameweek ${gw}: `,md+=`${hero.gwPoints} points from ${heroLine} `,md+=`${hero.gwHome?"at home to":"away at"} ${hero.gwOpponent}, in ${hero.gwMinutes} minutes. `,md+=`He went into the round owned by ${hero.own}% of managers.

`;const blanked=[...rows].filter(r=>r.own>=15).sort((a,b)=>a.gwPoints-b.gwPoints).filter(r=>r.gwPoints<=2).slice(0,4),takeaways=[`**${hero.name}** top-scored with ${hero.gwPoints} against ${hero.gwOpponent}.`,ev&&ev.average_entry_score?`The average manager scored **${ev.average_entry_score}**; the best score in the game was **${ev.highest_score}**.`:null,blanked.length?`**${blanked[0].name}** (${blanked[0].own}% owned) returned ${blanked[0].gwPoints} \u2014 the round's most expensive blank.`:null];if(ev&&ev.average_entry_score){const chips=(ev.chip_plays||[]).filter(c=>c.num_played>0).sort((a,b)=>b.num_played-a.num_played),entrants=ev.ranked_count||sdBoot?.total_players||0,capt=ev.most_captained?sdPlayerById(ev.most_captained):null,most=ev.most_selected?sdPlayerById(ev.most_selected):null;takeaways.push(chips.length?`**${chips[0].num_played.toLocaleString()}** managers played a ${SD_CHIP_NAME[chips[0].chip_name]||chips[0].chip_name}.`:null),md+=sdTakeaways(takeaways),md+=`## The round in numbers

`,md+="These are the figures FPL publishes about the gameweek itself rather than about any one player, ",md+=`and they set the bar every squad is measured against.

`;const numbers=[["Average score",`**${ev.average_entry_score}**`,"What the field scored"],["Highest score",`**${ev.highest_score||"\u2014"}**`,"The best single squad in the game"],["Managers ranked",entrants?entrants.toLocaleString():"\u2014","Squads that scored this round"]];capt&&numbers.push(["Most captained",`**${capt.name}**`,`${capt.own}% owned, returned ${(rows.find(r=>r.id===capt.id)||{}).gwPoints??"\u2014"}`]),most&&numbers.push(["Most selected",`**${most.name}**`,`${most.own}% owned`]),md+=sdTable(["Metric","Value","What it means"],numbers),chips.length&&(md+=`
### Chips played

`,md+=sdTable(["Chip","Managers","Share of the field"],chips.map(ch=>[SD_CHIP_NAME[ch.chip_name]||ch.chip_name,`**${ch.num_played.toLocaleString()}**`,entrants?`${sdRound(ch.num_played/entrants*100,1)}%`:"\u2014"])),md+=`
${SD_CHIP_NAME[chips[0].chip_name]||chips[0].chip_name} was the round's most-played chip. `,md+="A chip played in the same week as everybody else converts a good week into an average one relative to the field, ",md+=`which is the argument for holding until your own squad is ready rather than until the template says go.

`)}else md+=sdTakeaways(takeaways);md+=`## Who actually scored

`,md+=sdTable(["Player","Team","Pos","Pts","Mins","G","A","Bonus","xGI","Owned"],top.map(p=>[p.name,p.team,p.pos,`**${p.gwPoints}**`,p.gwMinutes,p.gwGoals,p.gwAssists,p.gwBonus,sdRound(p.gwXGI),`${p.own}%`])),blanked.length&&(md+=`
`,md+=sdCallout("warning",`The other side of the same weekend: ${blanked.map(b=>`**${b.name}** (${b.own}% owned) returned ${b.gwPoints}`).join(", ")}. Points you do not score cost exactly as much as points you do, and a blank from a widely-owned player is the one everybody feels at once.`));const finishers=rows.filter(r=>r.gwMinutes>=45&&r.gwXG>=.2).map(r=>({...r,diff:r.gwGoals-r.gwXG})).sort((a,b)=>b.diff-a.diff),over=finishers.slice(0,6),under=[...finishers].reverse().slice(0,6).filter(r=>r.diff<-.2);md+=`## The scoreline against the underlying data

`,md+="Expected goals measure the chances a player got into. Goals measure what he did with them. ",md+=`Over one round the gap between the two is mostly noise, but it is the right place to look for the returns that have not landed yet.

`,over.length&&(md+=`### Beat their chances

`,md+=sdTable(["Player","Team","Goals","xG","Difference","Mins"],over.map(p=>[p.name,p.team,p.gwGoals,sdRound(p.gwXG),`**+${sdRound(p.diff)}**`,p.gwMinutes]))),under.length&&(md+=`
### Created more than they took

`,md+=sdTable(["Player","Team","Goals","xG","Difference","Mins"],under.map(p=>[p.name,p.team,p.gwGoals,sdRound(p.gwXG),`**${sdRound(p.diff)}**`,p.gwMinutes])),md+=`
**${under[0].name}** generated ${sdRound(under[0].gwXG)} expected goals and scored ${under[0].gwGoals}. `,md+=`That is the kind of return that tends to arrive late rather than never \u2014 provided the chances keep coming.

`),md+=sdCallout("data",`One round is one round. Everything above describes what happened in Gameweek ${gw}; none of it is a verdict on anybody's finishing. A single game of expected-goals data has an error bar wide enough to drive a bus through. Ask again after five.`);const teamGoals={};(sdFixtures||[]).filter(f=>f.event===gw&&f.team_h_score!==null).forEach(f=>{teamGoals[f.team_h]={for:f.team_h_score,against:f.team_a_score},teamGoals[f.team_a]={for:f.team_a_score,against:f.team_h_score}});const cleanSheets=Object.entries(teamGoals).filter(([,g])=>g.against===0).map(([id])=>sdTeamName(parseInt(id,10)));md+=`## The defensive audit

`,md+=cleanSheets.length?`${cleanSheets.length} team${cleanSheets.length===1?"":"s"} kept a clean sheet in Gameweek ${gw}: ${cleanSheets.join(", ")}. `:`No team kept a clean sheet in Gameweek ${gw}. `,md+=`Clean sheets are worth four points to a defender and a goalkeeper, which makes the defensive picture worth as much attention as the attacking one.

`;const keepers=rows.filter(r=>r.pos==="GK"&&r.gwMinutes>=60).sort((a,b)=>b.gwPoints-a.gwPoints).slice(0,6);keepers.length&&(md+=`### Goalkeepers

`,md+=sdTable(["Player","Team","Price","Pts","Saves","Conceded","Owned"],keepers.map(p=>{const h=(sdHistory?.players||[]).find(x=>x.id===p.id),rec=h?(h.history||[]).find(g=>g.round===gw):null;return[p.name,p.team,`\xA3${p.price.toFixed(1)}m`,`**${p.gwPoints}**`,rec?rec.saves:"\u2014",rec?rec.goals_conceded:"\u2014",`${p.own}%`]})),md+=`
A goalkeeper behind a leaky defence is not automatically a bad pick: save points accumulate in exactly the weeks a clean sheet does not arrive, `,md+=`which is why the busiest keeper in the division is often better value than the one behind the best defence.

`);const defenders=rows.filter(r=>r.pos==="DEF"&&r.gwMinutes>=60).sort((a,b)=>b.gwPoints-a.gwPoints).slice(0,6);defenders.length&&(md+=`### Defenders

`,md+=sdTable(["Player","Team","Price","Pts","Bonus","xGI","Owned"],defenders.map(p=>[p.name,p.team,`\xA3${p.price.toFixed(1)}m`,`**${p.gwPoints}**`,p.gwBonus,sdRound(p.gwXGI),`${p.own}%`])),md+=`
`);const scored=Object.entries(teamGoals).map(([id,g])=>({id:parseInt(id,10),name:sdTeamName(parseInt(id,10)),...g})).sort((a,b)=>b.for-a.for);scored.length&&(md+=`### Attacking output by team

`,md+=sdTable(["Team","Scored","Conceded","Result"],scored.slice(0,8).map(t=>[`**${t.name}**`,t.for,t.against,t.for>t.against?"Won":t.for===t.against?"Drew":"Lost"])),md+=`
**${scored[0].name}** scored the most goals of the round with ${scored[0].for}. `,md+="Team-level attacking output is the cleanest signal available after a single round: it takes eleven players to create a goal and one to finish it, ",md+=`and the finisher changes far more often than the creating does.

`);const bonusMen=rows.filter(r=>r.gwBonus>0).sort((a,b)=>b.gwBonus-a.gwBonus||b.gwBps-a.gwBps).slice(0,10);bonusMen.length&&(md+=`## The bonus point picture

`,md+="Bonus is decided by the Bonus Points System, which rewards goals and assists heavily but also pays for tackles, recoveries, clean sheets and passing accuracy. ",md+=`Over a season it is worth roughly a point a game to the players who consistently rank in it, and it is the most overlooked component of a captaincy decision.

`,md+=sdTable(["Player","Team","Pos","Bonus","BPS","Pts","Owned"],bonusMen.map(p=>[`**${p.name}**`,p.team,p.pos,`**${p.gwBonus}**`,p.gwBps,p.gwPoints,`${p.own}%`])),md+=`
**${bonusMen[0].name}** took the maximum with ${bonusMen[0].gwBonus} from a BPS score of ${bonusMen[0].gwBps}. `,md+="Defenders and holding midfielders who accumulate BPS through defensive actions are the ones worth identifying early, ",md+=`because that route to bonus is far more repeatable than the one that runs through scoring goals.

`);const premiums=rows.filter(r=>r.price>=9&&r.position>=3).sort((a,b)=>b.own-a.own).slice(0,6);if(premiums.length>=2){md+=`## The premium watch

`,md+="A premium at \xA310m or more is not competing with the other forwards. He is competing with the two mid-price players you could field instead, ",md+=`and with the ${premiums[0].own}% of the field who already own the most popular of them.

`,md+=sdTable(["Player","Team","Price","Pts","Mins","G","A","xGI","Owned"],premiums.map(p=>[p.name,p.team,`\xA3${p.price.toFixed(1)}m`,`**${p.gwPoints}**`,p.gwMinutes,p.gwGoals,p.gwAssists,sdRound(p.gwXGI),`${p.own}%`]));const bestPrem=[...premiums].sort((a,b)=>b.gwPoints-a.gwPoints)[0],worstPrem=[...premiums].sort((a,b)=>a.gwPoints-b.gwPoints)[0];md+=`
**${bestPrem.name}** led the bracket with ${bestPrem.gwPoints}. **${worstPrem.name}** returned ${worstPrem.gwPoints}, `,md+=`and at \xA3${worstPrem.price.toFixed(1)}m that is the single largest hole a squad can have in a week. `,md+=`Not owning the most-selected premium is an active bet against ${premiums[0].own}% of the field \u2014 a legitimate one, but it should be deliberate.

`}const gems=rows.filter(r=>r.price<=5.5&&r.gwMinutes>=60&&r.gwPoints>=4).sort((a,b)=>b.gwPoints-a.gwPoints).slice(0,8);gems.length&&(md+=`## Enablers and budget gems

`,md+="The cheapest player who actually starts is worth more than a better player who does not. ",md+=`An enabler's job is to cost \xA34.5m and play ninety minutes; everything past that is profit that funds the rest of the squad.

`,md+=sdTable(["Player","Team","Pos","Price","Pts","Mins","Owned"],gems.map(p=>[p.name,p.team,p.pos,`\xA3${p.price.toFixed(1)}m`,`**${p.gwPoints}**`,p.gwMinutes,`${p.own}%`])),md+=`
**${gems[0].name}** at \xA3${gems[0].price.toFixed(1)}m returned ${gems[0].gwPoints} points from ${gems[0].gwMinutes} minutes, `,md+=`on ${gems[0].own}% ownership. Enablers at that price are how squads afford two premiums without hollowing out the bench.

`);const diffWinners=rows.filter(r=>r.own<8&&r.gwPoints>=6).sort((a,b)=>b.gwPoints-a.gwPoints).slice(0,8);diffWinners.length&&(md+=`## The differentials who paid

`,md+="Points from a player most of the field does not own are worth more than the same points from one they do. ",md+=`These returned at least six while owned by under 8% of managers, which is where rank is actually won.

`,md+=sdTable(["Player","Team","Pos","Price","Pts","Owned","Mins"],diffWinners.map(p=>[`**${p.name}**`,p.team,p.pos,`\xA3${p.price.toFixed(1)}m`,`**${p.gwPoints}**`,`${p.own}%`,p.gwMinutes])),md+=`
**${diffWinners[0].name}** at ${diffWinners[0].own}% ownership returned ${diffWinners[0].gwPoints}. `,md+="The managers who owned him gained ground on almost the entire field in a single afternoon \u2014 that is the whole case for carrying one or two genuine differentials ",md+=`rather than a squad indistinguishable from the template.

`);const heroStrip=sdTeamFixtures(hero.teamId,sdUpcomingGws(4)).map(f=>({gw:`GW${f.gw}`,opponent:`${f.opponent} ${f.home?"(H)":"(A)"}`,difficulty:f.difficulty}));if(heroStrip.length){md+=`## Is ${hero.name} worth chasing?

`,md+="The reflex after a haul is to buy it, and the reflex is usually wrong \u2014 you pay a risen price for a performance that has already happened. ",md+=`The question worth asking is not what he just did but what is in front of him:

`,md+=sdFixtureStrip(heroStrip);const heroAvgFdr=heroStrip.reduce((s,f)=>s+f.difficulty,0)/heroStrip.length;md+=`An average difficulty of ${sdRound(heroAvgFdr,1)} across his next four. `,md+=heroAvgFdr<=2.5?`That is a genuinely kind run, and it is the version of this question where chasing the haul is defensible.

`:heroAvgFdr>=3.6?`That is a hard run, and buying into it at a raised price is how managers end up transferring the same player out three weeks later.

`:`That is an ordinary run \u2014 not a reason to buy and not a reason to avoid, which usually means the transfer is better spent elsewhere.

`}const buys=rows.filter(r=>r.own<12&&r.gwMinutes>=60&&r.gwXGI>=.3).sort((a,b)=>b.gwXGI-a.gwXGI).slice(0,2),hold=rows.filter(r=>r.own>=20&&r.gwPoints<=3&&r.gwXGI>=.2).sort((a,b)=>b.gwXGI-a.gwXGI)[0],sell=rows.filter(r=>r.own>=10&&r.gwMinutes<45).sort((a,b)=>a.gwMinutes-b.gwMinutes)[0];md+=`## The early transfer radar

`,md+=`Four calls off the back of this round, with the reasoning attached so you can disagree with the reasoning rather than the conclusion.

`,md+=sdActions([...buys.map(b=>({verdict:"buy",subject:`${b.name} (${b.team}, \xA3${b.price.toFixed(1)}m)`,reason:`${sdRound(b.gwXGI)} expected involvements in ${b.gwMinutes} minutes and still only ${b.own}% owned.`})),hold?{verdict:"hold",subject:`${hold.name} (\xA3${hold.price.toFixed(1)}m)`,reason:`${hold.gwPoints} points reads badly, but ${sdRound(hold.gwXGI)} expected involvements says the process was fine. Selling here sells the scoreline, not the player.`}:null,sell?{verdict:"sell",subject:`${sell.name} (\xA3${sell.price.toFixed(1)}m)`,reason:sell.status!=="a"&&sell.news?sell.news:`${sell.gwMinutes} minutes. Minutes are the one thing no projection can work around.`}:null]),buys.length||(md+="Nothing in this round separated itself enough on the underlying numbers to be worth a transfer on its own. ",md+=`Sometimes the right move is none, and a saved transfer is worth more than a marginal one.

`);const nextGw=sdNextGw(),nextFixtures=(sdFixtures||[]).filter(f=>f.event===nextGw).slice(0,10);if(nextFixtures.length){md+=`## Looking ahead to Gameweek ${nextGw}

`,md+=sdTable(["Fixture","Home difficulty","Away difficulty"],nextFixtures.map(f=>[`${sdTeamName(f.team_h)} v ${sdTeamName(f.team_a)}`,sdFdrCell(f.team_h_difficulty),sdFdrCell(f.team_a_difficulty)]));const easiest=[...nextFixtures].sort((a,b)=>Math.min(a.team_h_difficulty,a.team_a_difficulty)-Math.min(b.team_h_difficulty,b.team_a_difficulty))[0],side=easiest.team_h_difficulty<=easiest.team_a_difficulty?sdTeamName(easiest.team_h):sdTeamName(easiest.team_a);md+=`
**${side}** have the kindest assignment of the round on difficulty alone. `,md+=`That is a starting point for captaincy rather than an answer \u2014 difficulty ratings know nothing about who is injured.
`}return{subject:sdSubject(hero,`${hero.gwPoints} pts \xB7 ${hero.gwGoals}G ${hero.gwAssists}A`),id:"gw-debrief",title:`Gameweek ${gw} Debrief: winners, blanks, and what the underlying numbers say`,category:"Gameweek Debrief",icon:"",dek:`${hero.name} led Gameweek ${gw} with ${hero.gwPoints} points${ev&&ev.average_entry_score?` against an average of ${ev.average_entry_score}`:""}. Underneath the scoreline, the expected-goals numbers point at a different set of names.`,body:md,featured:!0,source:`Generated from Gameweek ${gw} player history and FPL's published round summary`}}function sdGenPreDeadlineCaptaincy(){const gw=sdNextGw(),fixtures=(sdFixtures||[]).filter(f=>f.event===gw);if(!fixtures.length)return null;const rounds=Math.max(1,sdRoundsPlayed()),teamFdr={};fixtures.forEach(f=>{teamFdr[f.team_h]={opponent:sdTeamName(f.team_a),oppId:f.team_a,home:!0,fdr:f.team_h_difficulty},teamFdr[f.team_a]={opponent:sdTeamName(f.team_h),oppId:f.team_h,home:!1,fdr:f.team_a_difficulty}});const pool=sdPlayers().filter(p=>p.position>=3&&p.status==="a"&&teamFdr[p.teamId]&&p.minutes>=45*rounds).map(p=>{const proj=sdProjectedPoints(p,gw);return{...p,fx:teamFdr[p.teamId],xp:proj.xp,modelled:proj.modelled}}).sort((a,b)=>b.xp-a.xp||b.points-a.points),modelled=pool.length>0&&pool[0].modelled;if(pool.length<4)return null;const template=pool.filter(p=>p.own>=15).slice(0,8),differential=pool.filter(p=>p.own<10&&p.fx.fdr<=3).slice(0,6),lead=template[0]||pool[0],ev=sdEvent(gw),lastDone=sdCompletedRound(),prevEv=lastDone?sdEvent(lastDone):null,lastCaptain=prevEv&&prevEv.most_captained?sdPlayerById(prevEv.most_captained):null;let md=`## The armband is the biggest call you make

`;if(md+="Captaincy doubles a score, which makes it worth more than most transfers. ",md+=`A ten-point swing between two obvious picks is routine; over a season the armband decides more rank than any single buy.

`,md+=sdTakeaways([lead?`**${lead.name}** is the safe pick: ${lead.own}% owned, ${lead.fx.home?"at home to":"away at"} ${lead.fx.opponent}.`:null,differential.length?`**${differential[0].name}** is the differential at ${differential[0].own}% ownership.`:null,ev&&ev.transfers_made?`**${ev.transfers_made.toLocaleString()}** transfers have already been made ahead of the deadline.`:null,lastCaptain?`Last round the field captained **${lastCaptain.name}**.`:null]),md+=`## The captaincy matrix

`,md+=modelled?`Ranked on projected points for this gameweek \u2014 the opponent's defence, the venue and his expected minutes are all already inside that number, which is why the order does not follow the season averages. The difficulty rating is FPL's own.

`:`Ranked on points per gameweek so far, with the fixture attached. The difficulty rating is FPL's own.

`,md+=sdTable(["Player","Team","Price","Opponent",modelled?"Proj. pts":"Pts/GW","Owned","FDR"],template.map(p=>[`**${p.name}**`,p.team,`\xA3${p.price.toFixed(1)}m`,`${p.fx.opponent} ${p.fx.home?"(H)":"(A)"}`,sdRound(modelled?p.xp:p.points/rounds,1),`${p.own}%`,sdFdrCell(p.fx.fdr)])),lead){const strip=sdTeamFixtures(lead.teamId,sdUpcomingGws(4)).map(f=>({gw:`GW${f.gw}`,opponent:`${f.opponent} ${f.home?"(H)":"(A)"}`,difficulty:f.difficulty}));md+=`
### ${lead.name}'s next four

`,md+=sdFixtureStrip(strip),md+=`**${lead.name}** ${lead.fx.home?"hosts":"travels to"} ${lead.fx.opponent} at a difficulty of ${lead.fx.fdr}. `,md+=`At ${lead.own}% ownership the armband on him is close to a non-decision: captaining the same player as most of the field neither gains nor loses rank, `,md+=`which is exactly why it is the correct default unless you have a reason to move.

`}differential.length&&(md+=`## The differential armband

`,md+="Captaining off-template is a leveraged bet: it wins more rank than it should when it lands and costs more than it should when it does not. ",md+=`These are the players with a kind fixture and an ownership low enough to make the bet worth taking.

`,md+=sdTable(["Player","Team","Price","Opponent",modelled?"Proj. pts":"Pts/GW","Owned","FDR"],differential.map(p=>[`**${p.name}**`,p.team,`\xA3${p.price.toFixed(1)}m`,`${p.fx.opponent} ${p.fx.home?"(H)":"(A)"}`,sdRound(modelled?p.xp:p.points/rounds,1),`${p.own}%`,sdFdrCell(p.fx.fdr)])),md+=`
`,md+=sdCallout("warning","Effective ownership \u2014 the figure that actually decides whether a captaincy gains you rank \u2014 depends on how many managers captain a player, not just how many own him. FPL does not publish that number, and nothing on this page estimates it. The ownership column above is ownership. Treat the gap between the two as unknown rather than small."));const allDef=(sdBoot?.teams||[]).flatMap(t=>[t.strength_defence_home,t.strength_defence_away]).filter(Boolean),shortlist=template.slice(0,5);if(shortlist.length&&(md+=`## The defences they are up against

`,md+="A captaincy pick is half about the player and half about who he is playing. FPL publishes its own defensive rating for every team, ",md+=`home and away separately, and it is the one piece of opponent information that does not need a season's worth of results behind it.

`,md+=sdTable(["Player","Opponent","Venue","Opponent's defence","Rating","FDR"],shortlist.map(p=>{const def=sdStrength(p.fx.oppId,"defence",!p.fx.home);return[`**${p.name}**`,p.fx.opponent,p.fx.home?"Home":"Away",sdStrengthWord(def,allDef),def??"\u2014",sdFdrCell(p.fx.fdr)]})),md+=`
`,shortlist.slice(0,4).forEach(p=>{const def=sdStrength(p.fx.oppId,"defence",!p.fx.home);md+=`### ${p.name} \u2014 ${p.team}, \xA3${p.price.toFixed(1)}m

`,md+=`${p.own}% of the field own him. He ${p.fx.home?"hosts":"travels to"} ${p.fx.opponent}, `,md+=`whose ${p.fx.home?"away":"home"} defence is ${sdStrengthWord(def,allDef)}. `,md+=modelled?`He projects ${sdRound(p.xp,1)} points for this fixture, against a season average of ${sdRound(p.points/rounds,1)} a gameweek`:`He is averaging ${sdRound(p.points/rounds,1)} points a gameweek so far`,md+=p.xGI>0?` on ${sdRound(p.xGI)} expected involvements`:"",md+=`.

`})),lastCaptain){const lastRows=sdRoundRows(lastDone),capRow=lastRows.find(r=>r.id===lastCaptain.id),bestLast=[...lastRows].sort((a,b)=>b.gwPoints-a.gwPoints)[0];if(md+=`## What the field did last time

`,md+=`The most-captained player in Gameweek ${lastDone} was **${lastCaptain.name}**`,md+=capRow?`, and he returned ${capRow.gwPoints} \u2014 ${capRow.gwPoints*2} with the armband on.`:".",md+=`

`,capRow&&bestLast){const swing=(bestLast.gwPoints-capRow.gwPoints)*2;md+=swing>0?`The best available return was **${bestLast.name}** on ${bestLast.gwPoints}, so the difference between the crowd's armband and the optimal one was ${swing} points. `:"That was also the best return available, so the crowd's armband was the optimal one. ",md+=`Chasing the optimal captain every week is not a strategy \u2014 nobody hits it \u2014 but the size of that gap is why the decision deserves more thought than it usually gets.

`}}const vice=template.filter(p=>p!==lead&&p.fx.fdr<=3)[0]||template[1];vice&&(md+=`## The vice-captaincy

`,md+="The vice only pays out when your captain does not play, which makes it a decision most managers make carelessly and then regret twice a season. ",md+="The rule that matters: your vice should kick off **after** your captain wherever possible, so that a captain benched at the last minute can still be covered \u2014 ",md+=`and he should never be a rotation risk himself, because a captain who does not play backed by a vice who also does not play is a guaranteed zero on a doubled slot.

`,md+=`**${vice.name}** (${vice.own}% owned, ${vice.fx.home?"home to":"away at"} ${vice.fx.opponent}) is the natural cover here.

`),md+=`## Three ways this goes wrong

`,md+="- **Captaining form into a wall.** A player in good touch against a strong defence away from home is a worse bet than an average player at home to a poor one. ",md+=`Form is a weaker signal than the fixture, and it is the one that feels more compelling.
`,md+="- **Chasing last week.** The player who returned fifteen last gameweek is not more likely to return this one, but he is more likely to be captained. ",md+=`That is the mechanism by which the field collectively buys high.
`,md+="- **Going differential for its own sake.** An off-template armband should be justified by the fixture, not by the ownership. ",md+=`If the only argument for a captain is that few people own him, that is an argument for a lottery ticket.

`;const hardFixture=pool.filter(p=>p.fx.fdr>=4&&p.own>=10).slice(0,3),rotationRisk=sdPlayers().filter(p=>teamFdr[p.teamId]&&p.own>=5&&p.minutes>0&&p.minutes<60*rounds&&p.status==="a").sort((a,b)=>a.minutes-b.minutes).slice(0,3);return md+=`## The bench dilemma

`,md+="Starting eleven decisions are quieter than captaincy and cost nearly as much. Two categories are worth a second look before the deadline: ",md+=`players with a genuinely hard fixture, and players whose minutes have not been reliable.

`,hardFixture.length&&(md+=`### Hard fixtures

`,md+=sdTable(["Player","Team","Opponent","FDR","Owned"],hardFixture.map(p=>[p.name,p.team,`${p.fx.opponent} ${p.fx.home?"(H)":"(A)"}`,sdFdrCell(p.fx.fdr),`${p.own}%`])),md+=`
`),rotationRisk.length&&(md+=`### Minutes not guaranteed

`,md+=sdTable(["Player","Team","Mins","Mins/GW","Owned","Status"],rotationRisk.map(p=>[p.name,p.team,p.minutes,sdRound(p.minutes/rounds,0),`${p.own}%`,p.news?p.news.slice(0,40):"Fit"])),md+=`
`),md+=sdActions([lead?{verdict:"start",subject:`${lead.name} with the armband`,reason:`${lead.fx.opponent} ${lead.fx.home?"at home":"away"}, difficulty ${lead.fx.fdr}, ${lead.own}% owned.`}:null,differential[0]?{verdict:"watch",subject:`${differential[0].name} as the leveraged option`,reason:`${differential[0].own}% owned into a difficulty-${differential[0].fx.fdr} fixture.`}:null,hardFixture[0]?{verdict:"bench",subject:`${hardFixture[0].name}`,reason:`Difficulty ${hardFixture[0].fx.fdr} against ${hardFixture[0].fx.opponent}.`}:null,rotationRisk[0]?{verdict:"bench",subject:`${rotationRisk[0].name}`,reason:`${sdRound(rotationRisk[0].minutes/rounds,0)} minutes a gameweek. Not a starter yet.`}:null]),md+=`> With ${rounds} gameweek${rounds===1?"":"s"} played, the points-per-gameweek column is a small sample. `,md+=modelled?`The projection above leans on the schedule instead, which is fixed and known from day one, and on the underlying numbers rather than the points they have so far produced.
`:`The fixture column is not \u2014 the schedule is fixed and known, which is why it carries more weight this early in a season than form does.
`,{subject:lead?sdSubject(lead,`${lead.own}% owned \xB7 ${lead.fx.home?"H":"A"} v ${lead.fx.opponent}`):null,id:"pre-deadline-captaincy",title:`Gameweek ${gw} Captaincy Matrix: the armband, the differentials, and the bench calls`,category:"Strategy",icon:"",dek:`${lead?lead.name:"The template pick"} leads the captaincy matrix for Gameweek ${gw}${lead?`, ${lead.fx.home?"at home to":"away at"} ${lead.fx.opponent}`:""}. The differentials, the hard fixtures, and the players whose minutes are not safe.`,body:md,source:`Generated from the Gameweek ${gw} fixture list and current form`}}function sdGenUnderTheHood(){const rounds=Math.max(1,sdRoundsPlayed()),pool=sdPlayers().filter(p=>p.own<10&&p.status==="a"&&p.minutes>=45*rounds&&p.xGI>0).map(p=>({...p,ret:p.goals+p.assists,gap:p.xGI-(p.goals+p.assists)})).sort((a,b)=>b.gap-a.gap);if(pool.length<3)return null;const waiting=pool.slice(0,8),converting=[...pool].sort((a,b)=>a.gap-b.gap).slice(0,6),lead=waiting[0];let md=`## The gap between chances and goals

`;md+="Expected goal involvements measure the positions a player gets into. Goals and assists measure what came of them. ",md+="When the first runs well ahead of the second, one of two things is true: the player has been unlucky, or he is not good enough to finish what he creates. ",md+=`Over a long enough run it is almost always the first, which is why the gap is worth buying before it closes rather than after.

`,md+=sdTakeaways([`**${lead.name}** (${lead.own}% owned) has ${sdRound(lead.xGI)} expected involvements and ${lead.ret} actual.`,"Every player here is under 10% owned \u2014 the gap is only worth anything while the field has not noticed.",`Based on ${rounds} gameweek${rounds===1?"":"s"} of data. Treat it as a watchlist, not a shopping list.`]),md+=`## Creating without converting

`,md+=sdTable(["Player","Team","Pos","Price","Owned","xGI","G+A","Waiting on","Mins"],waiting.map(p=>[`**${p.name}**`,p.team,p.pos,`\xA3${p.price.toFixed(1)}m`,`${p.own}%`,sdRound(p.xGI),p.ret,`**${sdRound(p.gap)}**`,p.minutes])),md+=`
### How to read this table

`,md+='The "waiting on" column is expected involvements minus actual returns. A figure of 1.0 means a player has generated the chances for one more goal or assist than he has produced. ',md+="It is not a promise that the goal is coming \u2014 it is a statement that the chances are being created, which is the part of the process a player controls. ",md+=`Finishing regresses toward the mean over a season; getting into the box does not.

`,md+="Ownership matters here as much as the gap. A player at 30% ownership whose returns are lagging is a hold, not a buy \u2014 you already have him, and so does everybody you are competing with. ",md+=`The same profile at 4% is a genuine edge, because you capture the correction and most of the field does not.

`,waiting.slice(0,5).forEach(p=>{const strip=sdTeamFixtures(p.teamId,sdUpcomingGws(3)).map(f=>({gw:`GW${f.gw}`,opponent:`${f.opponent} ${f.home?"(H)":"(A)"}`,difficulty:f.difficulty}));md+=`### ${p.name} \u2014 ${p.team}, \xA3${p.price.toFixed(1)}m, ${p.own}% owned

`,md+=`${sdRound(p.xGI)} expected involvements across ${p.minutes} minutes, with ${p.ret} to show for it. `,md+=`His threat index sits at ${sdRound(p.threat||0,0)} and creativity at ${sdRound(p.creativity||0,0)}`,md+=p.bonus>0?`, and he has already picked up ${p.bonus} bonus point${p.bonus===1?"":"s"}`:"",md+=`.

`,strip.length&&(md+=sdFixtureStrip(strip))});const byPos=["DEF","MID","FWD"].map(pos=>{const inPos=pool.filter(p=>p.pos===pos);return inPos.length?{pos,n:inPos.length,gap:inPos.reduce((s,p)=>s+p.gap,0)/inPos.length,best:inPos.sort((a,b)=>b.gap-a.gap)[0]}:null}).filter(Boolean);if(byPos.length&&(md+=`## Where the gap is concentrated

`,md+="The same analysis by position, because the correction is worth different amounts depending on where a player lines up. ",md+=`An unconverted chance is worth six points to a defender and four to a forward, and defenders carry clean-sheet points on top.

`,md+=sdTable(["Position","Players under 10% owned","Average gap","Widest gap"],byPos.map(x=>[`**${x.pos}**`,x.n,sdRound(x.gap),`${x.best.name} (${sdRound(x.best.gap)})`])),md+=`
`),converting.length){md+=`## Taking more than they create

`,md+="The mirror image, and the more dangerous group to own. These players have converted above the rate their chances imply. ",md+="That is not an accusation \u2014 finishing is a real skill \u2014 but a player whose returns exceed his underlying numbers is priced on the returns, ",md+=`and the underlying numbers are what continue.

`,md+=sdTable(["Player","Team","Price","Owned","xGI","G+A","Above expectation"],converting.map(p=>[p.name,p.team,`\xA3${p.price.toFixed(1)}m`,`${p.own}%`,sdRound(p.xGI),p.ret,`**+${sdRound(-p.gap)}**`])),md+=`
**${converting[0].name}** has returned ${sdRound(-converting[0].gap)} more than his chances imply. `,md+="That is not a sell signal on its own \u2014 a player who keeps getting into good positions will keep scoring even after the finishing cools \u2014 ",md+=`but it does mean the price you are paying reflects the best case rather than the underlying case.

`;const cStrip=sdTeamFixtures(converting[0].teamId,sdUpcomingGws(3)).map(f=>({gw:`GW${f.gw}`,opponent:`${f.opponent} ${f.home?"(H)":"(A)"}`,difficulty:f.difficulty}));cStrip.length&&(md+=sdFixtureStrip(cStrip))}md+=`## The minutes question

`,md+="Every name above is only worth anything if he keeps starting. A player creating chances from sixty minutes a week is a different proposition to one doing it from ninety, ",md+=`and the low ownership that makes these players interesting is often low precisely because their place is not secure.

`,md+=sdTable(["Player","Team","Minutes","Mins per GW","Starts","Status"],waiting.slice(0,6).map(p=>[`**${p.name}**`,p.team,p.minutes,sdRound(p.minutes/rounds,0),p.starts,p.status==="a"?"Fit":p.news?p.news.slice(0,34):"Doubt"]));const nailed=waiting.filter(p=>p.minutes/rounds>=80);return md+=`
${nailed.length} of these ${waiting.length} are playing 80 minutes or more a gameweek. `,md+=`Those are the ones where the gap between chances and returns is a genuine buying opportunity; the rest are watchlist entries whose first requirement is a run in the side.

`,md+=sdCallout("data","Expected goals is a model, not a measurement. It assigns every shot the conversion rate of an average player from that position, which systematically undersells elite finishers and oversells poor ones. Read the gap as a question worth asking, not an answer."),md+=sdActions([...waiting.slice(0,2).map(p=>({verdict:"buy",subject:`${p.name} (${p.team}, \xA3${p.price.toFixed(1)}m)`,reason:`${sdRound(p.gap)} expected involvements not yet converted, at ${p.own}% ownership.`})),converting[0]?{verdict:"watch",subject:`${converting[0].name} (\xA3${converting[0].price.toFixed(1)}m)`,reason:`Returning ${sdRound(-converting[0].gap)} above what his chances imply. Priced on the returns.`}:null]),{subject:sdSubject(lead,`${sdRound(lead.xGI)} xGI \xB7 ${lead.ret} returns`),id:"under-the-hood",title:"Under the Hood: the low-owned players creating chances without the returns",category:"Data Deep-Dive",icon:"",dek:`${lead.name} has ${sdRound(lead.xGI)} expected involvements and ${lead.ret} to show for it, on ${lead.own}% ownership. The gap between process and result is where differentials are found.`,body:md,source:`Generated from expected involvement against actual returns across ${rounds} gameweek${rounds===1?"":"s"}`}}function sdGenInsideAlgorithm(){let md=`## What the projection actually does

`;return md+=`Every projected-points figure on this site comes from one function. It is worth being specific about what it does, because "AI" is doing a lot of unearned work in most FPL tools.

`,md+=`### 1. Minutes first

`,md+="Nothing else matters if a player does not play. The model estimates a probability of starting from minutes per appearance, then blends a starter's minutes with a substitute's cameo. ",md+=`A player with a fitness doubt has that probability scaled by his published chance of playing.

`,md+=`### 2. Attacking returns, regressed

`,md+="Per-90 rates from a small sample are wild \u2014 one goal in one appearance is a huge xG/90. ",md+=`The model regresses each player's rate toward his position's average until there is roughly five full matches of evidence, then lets his own numbers stand alone.

`,md+=`### 3. Clean sheets from the defence, not the fixture alone

`,md+="Clean-sheet probability comes from goals conceded and a defensive rating, adjusted by fixture difficulty, and is capped at both ends. ",md+=`No defence is a certainty and none is hopeless.

`,md+=`### 4. Then the scoring rules

`,md+=sdTable(["Component","How it is derived"],[["Appearance","Start probability against 2 points for 60 minutes, 1 otherwise"],["Attack","Regressed xG and xA per 90, scaled by expected minutes and fixture"],["Clean sheet","Clean-sheet probability by position value, scaled by start probability"],["Saves","Saves per 90 divided by 3, for goalkeepers"],["Bonus","Bonus per game, capped"],["Conceded","Goals conceded against the chance of no clean sheet, as a negative"]]),md+=`
### What it deliberately does not do

`,md+="The model uses FPL's own `ep_next` only as a floor while a player has little history, and only for the immediate gameweek. ",md+=`Three gameweeks out it is ignored entirely \u2014 it is an estimate for the next match and means nothing beyond it.

`,md+="> The projection is calibrated against FPL's published average entry score. It is a model, not a prophecy, and the numbers it produces are only as good as the sample behind them. ",md+=`Early in a season that sample is small, which is why this site says "too early to rank" rather than inventing a ranking.
`,{id:"inside-algorithm",title:"Inside the Algorithm: How our FPL AI predicts points",category:"Behind the Build",icon:"",dek:"No black box. A component-by-component walkthrough of how every projected-points number on this site is produced, and what the model refuses to claim.",body:md,source:"Describes the projection model in scripts/pitch-snapshot.js"}}function sdGenFixtureHorizon(){const gws=sdUpcomingGws(6);if(gws.length<4)return null;const half=Math.floor(gws.length/2),teams=sdBoot?.teams||[];if(!teams.length)return null;const rows=teams.map(t=>{const fx=sdTeamFixtures(t.id,gws),avg=list=>list.length?list.reduce((s,f)=>s+f.difficulty,0)/list.length:null,near=avg(fx.filter(f=>gws.slice(0,half).includes(f.gw))),far=avg(fx.filter(f=>gws.slice(half).includes(f.gw))),homes=fx.filter(f=>f.home).length;return near==null||far==null?null:{id:t.id,name:t.name,short:t.short_name,fx,near,far,swing:near-far,all:avg(fx),homes,games:fx.length}}).filter(Boolean);if(rows.length<10)return null;const easing=[...rows].sort((a,b)=>b.swing-a.swing).slice(0,4),hardening=[...rows].sort((a,b)=>a.swing-b.swing).slice(0,4),kindest=[...rows].sort((a,b)=>a.all-b.all).slice(0,5),harshest=[...rows].sort((a,b)=>b.all-a.all).slice(0,3);let md=`## The next six weeks

`;md+="FPL is played in blocks, not in single weeks. The teams worth buying are rarely the ones with the best fixture on Saturday \u2014 ",md+="they are the ones whose schedule turns over the next month, bought before the turn rather than after it. ",md+=`This is the run from Gameweek ${gws[0]} to Gameweek ${gws[gws.length-1]}.

`,md+=sdTakeaways([`**${easing[0].name}** have the sharpest improvement: ${sdRound(easing[0].near,1)} average difficulty becoming ${sdRound(easing[0].far,1)}.`,`**${hardening[0].name}** go the other way \u2014 ${sdRound(hardening[0].near,1)} becoming ${sdRound(hardening[0].far,1)}.`,`**${kindest[0].name}** have the easiest six-week run overall at ${sdRound(kindest[0].all,1)}.`]),md+=`## The full six-gameweek matrix

`,md+=`Every team, every fixture, coloured by FPL's own difficulty rating. Home fixtures are marked (H).

`;const matrix=[...rows].sort((a,b)=>a.all-b.all).map(t=>{const cells=gws.map(g=>{const f=t.fx.find(x=>x.gw===g);return f?`${f.opponent} ${f.home?"(H)":"(A)"}`:"\u2014"});return[`**${t.short}**`,...cells,sdFdrCell(t.all)]});md+=sdTable(["Team",...gws.map(g=>`GW${g}`),"Avg"],matrix),md+=`
## Schedules easing

`,md+=sdTable(["Team",`GW${gws[0]}\u2013${gws[half-1]}`,`GW${gws[half]}\u2013${gws[gws.length-1]}`,"Swing","Home games"],easing.map(t=>[`**${t.name}**`,sdFdrCell(t.near),sdFdrCell(t.far),`**\u2212${sdRound(t.swing,1)}**`,`${t.homes}/${t.games}`]));const e0=easing[0];md+=`
**${e0.name}** have the sharpest turn: an average difficulty of ${sdRound(e0.near,1)} across the first block against ${sdRound(e0.far,1)} in the second. `,md+=`Their run reads:

`,md+=sdFixtureStrip(e0.fx.map(f=>({gw:`GW${f.gw}`,opponent:`${f.opponent} ${f.home?"(H)":"(A)"}`,difficulty:f.difficulty}))),md+="The window to buy into a swing like that is while the hard fixtures are still being played, because that is when the price is still low ",md+=`and the ownership has not moved.

`,md+=`## Schedules hardening

`,md+=sdTable(["Team",`GW${gws[0]}\u2013${gws[half-1]}`,`GW${gws[half]}\u2013${gws[gws.length-1]}`,"Swing","Home games"],hardening.map(t=>[`**${t.name}**`,sdFdrCell(t.near),sdFdrCell(t.far),`**+${sdRound(-t.swing,1)}**`,`${t.homes}/${t.games}`]));const h0=hardening[0];md+=`
**${h0.name}** go the other way:

`,md+=sdFixtureStrip(h0.fx.map(f=>({gw:`GW${f.gw}`,opponent:`${f.opponent} ${f.home?"(H)":"(A)"}`,difficulty:f.difficulty}))),md+=`If you hold their assets, the exit window is now rather than in three weeks when the fixtures have turned and everybody is selling at once.

`,md+=`## Best and worst runs overall

`,md+=sdTable(["Team","Six-week average","Home games","Fixtures"],kindest.map(t=>[`**${t.name}**`,sdFdrCell(t.all),`${t.homes}/${t.games}`,t.fx.map(f=>`${f.opponent}${f.home?"":" (A)"}`).join(", ")])),md+=`
**${kindest[0].name}** have the kindest overall run at ${sdRound(kindest[0].all,1)}, with ${kindest[0].homes} of ${kindest[0].games} at home. `,md+=`**${harshest[0].name}** have the hardest at ${sdRound(harshest[0].all,1)}. `,md+=`Home advantage is worth roughly a fifth of a goal in most models, which is small per match and considerable across six.

`;const doubles=rows.filter(t=>t.games>gws.length),blanks=rows.filter(t=>t.games<gws.length);md+=`## Schedule density

`,doubles.length||blanks.length?(doubles.length&&(md+=`${doubles.map(t=>`**${t.name}**`).join(", ")} play more than one fixture in a gameweek in this window. `),blanks.length&&(md+=`${blanks.map(t=>`**${t.name}**`).join(", ")} have a gameweek without a fixture. `),md+=`Both change the arithmetic considerably: a doubled asset is two chances at a return in one week, and a blank is a guaranteed zero from a starting slot.

`):(md+=`Every team plays exactly ${gws.length} fixtures across this window \u2014 no doubles, no blanks. `,md+=`The schedule is even, so the fixture question here is purely about difficulty rather than volume.

`),md+=`## Home and away

`,md+="The same six fixtures are worth materially different amounts depending on where they are played. ",md+=`Home advantage in the Premier League is worth somewhere around a fifth of a goal a game \u2014 small in isolation, and the difference between a good run and an ordinary one across a block.

`;const homeHeavy=[...rows].sort((a,b)=>b.homes-a.homes).slice(0,4),awayHeavy=[...rows].sort((a,b)=>a.homes-b.homes).slice(0,4);return md+=sdTable(["Team","Home fixtures","Away fixtures","Six-week difficulty"],homeHeavy.map(t=>[`**${t.name}**`,t.homes,t.games-t.homes,sdFdrCell(t.all)])),md+=`
**${homeHeavy[0].name}** play ${homeHeavy[0].homes} of their ${homeHeavy[0].games} fixtures at home in this window. `,md+=`At the other end, **${awayHeavy[0].name}** play only ${awayHeavy[0].homes}, which drags a schedule that looks fine on difficulty alone.

`,md+=`## The three sharpest turns

`,easing.slice(0,4).forEach(t=>{md+=`### ${t.name}

`,md+=`Difficulty of ${sdRound(t.near,1)} across GW${gws[0]}\u2013${gws[half-1]} against ${sdRound(t.far,1)} across GW${gws[half]}\u2013${gws[gws.length-1]}, `,md+=`with ${t.homes} of ${t.games} at home.

`,md+=sdFixtureStrip(t.fx.map(f=>({gw:`GW${f.gw}`,opponent:`${f.opponent} ${f.home?"(H)":"(A)"}`,difficulty:f.difficulty})))}),md+=sdActions([{verdict:"buy",subject:`${easing[0].name} assets`,reason:`Difficulty falls from ${sdRound(easing[0].near,1)} to ${sdRound(easing[0].far,1)} across the window.`},{verdict:"buy",subject:`${kindest[0].name} assets`,reason:`Kindest six-week run in the division at ${sdRound(kindest[0].all,1)}, ${kindest[0].homes} of them at home.`},{verdict:"sell",subject:`${hardening[0].name} assets`,reason:`Difficulty rises from ${sdRound(hardening[0].near,1)} to ${sdRound(hardening[0].far,1)}. Sell into the good run, not out of the bad one.`}]),md+=`## What a swing is worth by position

`,md+=`A fixture swing does not pay every position equally, which is why "buy the good fixtures" is incomplete advice.

`,md+=`- **Defenders and goalkeepers** gain the most. Clean sheets are close to binary and heavily fixture-dependent: a defender facing the weakest attacks in the division is a different asset to the same defender facing the strongest, and the four points arrive all at once.
`,md+=`- **Midfielders** gain least from easy fixtures and lose least from hard ones. Attacking returns are driven more by the player's own involvement than by the opponent, and the good ones create chances against anybody.
`,md+=`- **Forwards** sit in between, with one caveat: a poor team's forward in a good run is still a poor team's forward. Fixture swings amplify a team's attacking quality rather than substituting for it.

`,md+=`The practical version is that a defensive asset is worth buying **into** a swing and selling out of it, while a genuine premium attacker is usually worth holding through a hard run rather than transferring twice.

`,md+=sdCallout("warning","Fixture difficulty is FPL's own rating, set before the season and only lightly revised. It knows nothing about injuries, form, or a manager change, and it rates the badge rather than the team currently wearing it. Across a six-week block it is directionally right more often than not, which is the most that can be claimed for it."),{id:"fixture-horizon",title:`The Fixture Horizon: who turns green through Gameweek ${gws[gws.length-1]}`,category:"Fixture Watch",icon:"",dek:`${easing[0].name} see their schedule ease sharply over the next six gameweeks while ${hardening[0].name} run into a wall. The full matrix, the swings, and where to buy before the turn.`,body:md,source:`Generated from fixture difficulty across GW${gws[0]}\u2013GW${gws[gws.length-1]}`}}function sdGenMarketDigest(){const players=sdPlayers(),total=sdBoot?.total_players||0;if(!total)return null;const withPressure=players.filter(p=>p.own>.3).map(p=>({...p,net:p.tIn-p.tOut,owners:Math.max(p.own/100*total,1)})).map(p=>({...p,pressure:p.net/p.owners}));if(withPressure.length<10)return null;const rising=[...withPressure].sort((a,b)=>b.pressure-a.pressure).slice(0,10),falling=[...withPressure].sort((a,b)=>a.pressure-b.pressure).slice(0,10),alreadyRisen=players.filter(p=>p.costChange>0).sort((a,b)=>b.costChange-a.costChange).slice(0,6),alreadyFallen=players.filter(p=>p.costChange<0).sort((a,b)=>a.costChange-b.costChange).slice(0,6),nextEv=sdEvent(sdNextGw());let md=`## How prices actually move

`;return md+="A player's price rises when enough managers buy him relative to how many already own him. ",md+="That last clause is what most price-watch coverage misses: a player owned by 300,000 managers needs a fraction of the buys that a player owned by four million does. ",md+=`Raw transfer counts therefore mislead in both directions, and the useful figure is net transfers divided by the existing ownership base.

`,md+=sdTakeaways([`**${rising[0].name}** is under the heaviest buying pressure relative to his ownership.`,`**${falling[0].name}** is being sold hardest.`,nextEv&&nextEv.transfers_made?`**${nextEv.transfers_made.toLocaleString()}** transfers have been made ahead of the next deadline.`:null,alreadyRisen.length?`**${alreadyRisen.length}** of the players tracked here have already risen this gameweek.`:null]),md+=`## Under buying pressure

`,md+=sdTable(["Player","Team","Price","Owned","Net transfers","Pressure"],rising.map(p=>[`**${p.name}**`,p.team,`\xA3${p.price.toFixed(1)}m`,`${p.own}%`,`**+${p.net.toLocaleString()}**`,sdRound(p.pressure*100,1)+"%"])),md+=`
**${rising[0].name}** leads on pressure: ${rising[0].net.toLocaleString()} net transfers in against an ownership base of roughly ${Math.round(rising[0].owners).toLocaleString()} managers. `,md+=`Buying before a rise is worth a tenth of a million, which is trivial on any single transfer and compounds into real squad value across a season.

`,md+=`## Being sold

`,md+=sdTable(["Player","Team","Price","Owned","Net transfers","Pressure"],falling.map(p=>[`**${p.name}**`,p.team,`\xA3${p.price.toFixed(1)}m`,`${p.own}%`,`**${p.net.toLocaleString()}**`,sdRound(p.pressure*100,1)+"%"])),md+=`
**${falling[0].name}** is the heaviest sell. A falling price is only a problem if you intend to sell him: `,md+=`the loss is realised at the point of transfer, not at the point of the drop, and panicking into a fall usually costs more than the fall did.

`,(alreadyRisen.length||alreadyFallen.length)&&(md+=`## Already moved this gameweek

`,alreadyRisen.length&&(md+=`### Risen

`,md+=sdTable(["Player","Team","New price","Change","Owned"],alreadyRisen.map(p=>[p.name,p.team,`\xA3${p.price.toFixed(1)}m`,`**+\xA3${p.costChange.toFixed(1)}m**`,`${p.own}%`]))),alreadyFallen.length&&(md+=`
### Fallen

`,md+=sdTable(["Player","Team","New price","Change","Owned"],alreadyFallen.map(p=>[p.name,p.team,`\xA3${p.price.toFixed(1)}m`,`**\xA3${p.costChange.toFixed(1)}m**`,`${p.own}%`]))),md+=`
`),md+=`## The movers in detail

`,rising.slice(0,4).forEach(p=>{const strip=sdTeamFixtures(p.teamId,sdUpcomingGws(3)).map(f=>({gw:`GW${f.gw}`,opponent:`${f.opponent} ${f.home?"(H)":"(A)"}`,difficulty:f.difficulty}));md+=`### ${p.name} \u2014 ${p.team}, \xA3${p.price.toFixed(1)}m

`,md+=`${p.net.toLocaleString()} net transfers in against an ownership base of about ${Math.round(p.owners).toLocaleString()}. `,md+=`He has ${p.points} points from ${p.minutes} minutes`,md+=p.xGI>0?` on ${sdRound(p.xGI)} expected involvements`:"",md+=`, and the market has decided that is worth buying.

`,strip.length&&(md+=sdFixtureStrip(strip))}),md+=`## Squad value risk

`,md+="Squad value is not a scoreboard. It is a constraint that decides which squads you can build later in the season, and it leaks quietly rather than dramatically. ",md+=`The mechanism is worth stating precisely, because it is where most of the confusion lives.

`,md+="When a player's price falls by a tenth, you lose a tenth of squad value immediately. When it rises by a tenth, you only bank half of the rise \u2014 ",md+="FPL charges 50% sell-on tax, rounded down. So a player bought at \xA37.0m who climbs to \xA37.4m sells for \xA37.2m, not \xA37.4m. ",md+=`The asymmetry means falls cost you more than rises earn you, and it is why avoiding falls beats chasing rises as a strategy.

`,md+=`### The four rules that actually matter

`,md+=`- **Never make a transfer to catch a price rise.** A tenth of a million is worth roughly nothing; a wasted transfer is worth four points.
`,md+=`- **Never hold a player you have decided to sell.** The fall is already priced in by the time you notice it, and waiting only deepens it.
`,md+=`- **Buy early in the week when you are confident, late when you are not.** Information arrives across the week \u2014 press conferences, training reports. Trading a tenth for a team-news update is a good trade.
`,md+=`- **Ignore value entirely until the second half of the season.** It is a constraint that binds late, not a score that runs throughout.

`,md+=sdActions([{verdict:"buy",subject:`${rising[0].name} (\xA3${rising[0].price.toFixed(1)}m)`,reason:`${rising[0].net.toLocaleString()} net transfers in. If you want him, the cheap window is now.`},rising[1]?{verdict:"watch",subject:`${rising[1].name} (\xA3${rising[1].price.toFixed(1)}m)`,reason:`${rising[1].net.toLocaleString()} net in and climbing.`}:null,{verdict:"sell",subject:`${falling[0].name} (\xA3${falling[0].price.toFixed(1)}m)`,reason:`${falling[0].net.toLocaleString()} net out. Only act if you were selling anyway \u2014 do not sell a player to save a tenth.`}]),md+=sdCallout("warning","FPL does not publish its price-change thresholds, and never has. The pressure column above is a model built from net transfers against the ownership base, not the real number, and it will be wrong at the margins. Nobody outside the game's developers knows the actual formula; anyone claiming otherwise has reverse-engineered an approximation too."),{subject:sdSubject(rising[0],`${(rising[0].tIn||0).toLocaleString()} bought in`),id:"market-digest",title:"Market Watch: who is rising, who is being sold, and what it costs to be late",category:"Market",icon:"",dek:`${rising[0].name} is under the heaviest buying pressure in the game and ${falling[0].name} is being sold hardest. Where the transfer market is heading before the prices catch up.`,body:md,source:"Generated from this gameweek's net transfers measured against ownership"}}function sdGenTacticalPlaybook(){const players=sdPlayers(),rounds=Math.max(1,sdRoundsPlayed()),total=sdBoot?.total_players||0;if(!players.length)return null;const chipTotals={};(sdBoot?.events||[]).forEach(e=>(e.chip_plays||[]).forEach(c=>{chipTotals[c.chip_name]=(chipTotals[c.chip_name]||0)+c.num_played}));const chipRows=Object.entries(chipTotals).filter(([,n])=>n>0).sort((a,b)=>b[1]-a[1]),windows=(sdBoot?.chips||[]).map(c=>({name:SD_CHIP_NAME[c.name]||c.name,start:c.start_event,stop:c.stop_event}));let md=`## Structure beats selection

`;md+="Most of the rank in FPL is decided by two things that have nothing to do with picking the right player in a given week: ",md+="how your budget is distributed across the squad, and when you play your chips. ",md+=`Both are structural decisions, both are made rarely, and both are worth more thought than the weekly transfer that gets all the attention.

`,md+=sdTakeaways([chipRows.length?`**${(chipTotals[chipRows[0][0]]||0).toLocaleString()}** managers have played a ${SD_CHIP_NAME[chipRows[0][0]]||chipRows[0][0]} so far.`:null,"Points per million is the only currency that matters, and it is not evenly distributed across price brackets.","Chip windows are fixed and published \u2014 the first half's chips expire, they do not roll over."]),chipRows.length&&(md+=`## What the field has done with its chips

`,md+=`These are real counts published by FPL, not estimates.

`,md+=sdTable(["Chip","Times played","Share of the field"],chipRows.map(([name,n])=>[`**${SD_CHIP_NAME[name]||name}**`,n.toLocaleString(),total?`${sdRound(n/total*100,2)}%`:"\u2014"])),md+=`
`,md+=sdCallout("tactical","A chip played in the same gameweek as everybody else converts a good week into an average one relative to the field. The Bench Boost that returns 18 points when 800,000 other managers also played theirs gains you almost nothing in rank. Chips are worth most when your squad is ready for them, which is rarely the week the template says go."));const chipPlan=[{key:"bboost",heading:"Bench Boost",what:"Scores your bench as well as your eleven, so it is worth exactly what your four worst players produce.",how:"It is the chip most damaged by playing it on schedule. A bench of two \xA34.0m defenders who do not start is worth perhaps six points; a bench built deliberately over three or four weeks \u2014 four players who all start, ideally in a gameweek where some teams play twice \u2014 is worth twenty-five or more. The chip does not create the value, the preparation does, and preparing costs transfers you have to plan for well in advance."},{key:"3xc",heading:"Triple Captain",what:"Trebles rather than doubles your captain, so it is worth one extra copy of whatever that player scores.",how:"The expected gain is roughly the player's average return, which means it is worth far less than managers assume \u2014 a premium averaging seven returns about seven extra points on a normal week. It is therefore a chip for a genuine outlier: a heavy favourite at home, ideally in a double gameweek, against a defence rated among the weakest in the division. Playing it on an ordinary week converts a rare resource into a routine one."},{key:"freehit",heading:"Free Hit",what:"Gives you a completely different squad for one gameweek, then reverts everything.",how:"Its highest use is a blank gameweek, where a normal squad might field seven players and a Free Hit squad fields eleven \u2014 that gap is worth more than any transfer strategy can recover. Using it to chase a good set of fixtures in a full gameweek is almost always a waste, because the alternative use is worth so much more later."},{key:"wildcard",heading:"Wildcard",what:"Unlimited transfers for one gameweek, permanently.",how:"The mistake is playing it in response to a bad week rather than in anticipation of a fixture swing. A wildcard played to fix four problems buys you four transfers you would have made anyway over a month; a wildcard played to pivot an entire squad onto a block of good fixtures buys you a structural advantage that compounds for six weeks."}].filter(cp=>chipTotals[cp.key]!==void 0||cp.key==="wildcard");chipPlan.length&&(md+=`## Chip by chip

`,chipPlan.forEach(cp=>{const played=chipTotals[cp.key]||0;md+=`### ${cp.heading}

`,md+=`${cp.what}`,md+=played>0?` **${played.toLocaleString()}** managers have played it so far${total?`, ${sdRound(played/total*100,2)}% of the field`:""}.`:" Nobody has played it yet this season.",md+=`

${cp.how}

`})),windows.length&&(md+=`## The chip calendar

`,md+="Each chip exists twice, once per half of the season, and the first-half version expires rather than rolling over. ",md+=`That deadline is the single most common way managers waste a chip.

`,md+=sdTable(["Chip","Available from","Expires after"],windows.map(w=>[`**${w.name}**`,`GW${w.start}`,`GW${w.stop}`])),md+=`
`);const bands=[{label:"Budget (under \xA35.5m)",min:0,max:5.4},{label:"Mid (\xA35.5m\u2013\xA37.4m)",min:5.5,max:7.4},{label:"Upper-mid (\xA37.5m\u2013\xA39.4m)",min:7.5,max:9.4},{label:"Premium (\xA39.5m and up)",min:9.5,max:99}],starters=players.filter(p=>p.minutes>=60*rounds*.7);md+=`## Where the value actually sits

`,md+="Every squad is the same 100 million distributed differently. This is what each price bracket returns, ",md+=`restricted to players who are actually starting \u2014 a bracket average that counts benchwarmers describes nothing you can buy.

`,md+=sdTable(["Price bracket","Starting players","Median points","Best","Points per \xA3m"],bands.map(b=>{const inBand=starters.filter(p=>p.price>=b.min&&p.price<=b.max).sort((x,y)=>y.points-x.points);if(!inBand.length)return[b.label,"0","\u2014","\u2014","\u2014"];const median=inBand[Math.floor(inBand.length/2)].points,best=inBand[0],ppm=inBand.reduce((s,p)=>s+p.points/p.price,0)/inBand.length;return[`**${b.label}**`,inBand.length,median,`${best.name} (${best.points})`,sdRound(ppm,2)]}));const byPos=["GK","DEF","MID","FWD"].map(pos=>{const inPos=starters.filter(p=>p.pos===pos).sort((a,b)=>b.points-a.points);return inPos.length?{pos,n:inPos.length,best:inPos[0],ppm:inPos.reduce((s,p)=>s+p.points/p.price,0)/inPos.length,avgPrice:inPos.reduce((s,p)=>s+p.price,0)/inPos.length}:null}).filter(Boolean);if(byPos.length){md+=`
## Value by position

`,md+=sdTable(["Position","Starters","Average price","Points per \xA3m","Top scorer"],byPos.map(x=>[`**${x.pos}**`,x.n,`\xA3${sdRound(x.avgPrice,1)}m`,sdRound(x.ppm,2),`${x.best.name} (${x.best.points})`]));const bestValue=[...byPos].sort((a,b)=>b.ppm-a.ppm)[0];md+=`
**${bestValue.pos}** currently offers the most points per million of any position at ${sdRound(bestValue.ppm,2)}. `,md+="That is the argument for shape: if one position is returning more per pound than the others, the correct formation is the one that fields more of it, ",md+=`and the transfer that matters is the one that moves budget into that bracket rather than sideways within it.

`}const template=players.filter(p=>p.own>=20).sort((a,b)=>b.own-a.own).slice(0,12);if(template.length){md+=`## The template

`,md+="These are the players a fifth or more of the field owns. Every one of them is a decision you are making whether you think about it or not: ",md+=`owning them is neutral, and not owning them is an active bet against that share of your competition every single gameweek.

`,md+=sdTable(["Player","Team","Pos","Price","Owned","Points"],template.map(p=>[`**${p.name}**`,p.team,p.pos,`\xA3${p.price.toFixed(1)}m`,`**${p.own}%**`,p.points]));const concentration=template.reduce((s,p)=>s+p.own,0);md+=`
Those ten players account for ${sdRound(concentration,0)} percentage points of combined ownership. `,md+="A squad that owns all of them cannot lose ground to the field, and cannot gain any either. ",md+=`The useful position is usually most of the template plus two or three genuine differentials \u2014 enough to move, not enough to be reckless.

`}const avgReturn=starters.length?starters.reduce((s,p)=>s+p.points,0)/starters.length/rounds:0;return md+=`## What a hit actually costs

`,md+="A transfer beyond your free one costs four points. That number only means something next to what a player actually returns, ",md+=`and the starting players in this dataset are averaging ${sdRound(avgReturn,1)} points a gameweek.

`,md+=`So a hit has to buy you roughly ${sdRound(4/Math.max(avgReturn,1),1)} gameweeks' worth of an average player's output just to break even \u2014 `,md+="and it has to do it against the player you sold, not against nothing. The real comparison is the difference between two players over the weeks you will hold the new one, ",md+=`which is why hits taken for a fixture swing tend to pay and hits taken to chase last week's haul tend not to.

`,md+=`- **One hit for a genuine upgrade held six weeks**: usually correct.
`,md+=`- **One hit to move sideways**: almost never correct.
`,md+=`- **Two hits in a week**: correct only when covering multiple injuries, and even then a wildcard is often the better answer.

`,md+=`## The constraint nobody designs around

`,md+="Every squad is fifteen players: two goalkeepers, five defenders, five midfielders and three forwards, with at most three from any one club. ",md+=`Only eleven of them score in a normal week. That means four squad slots exist purely to be legal, and how much you spend on them is one of the few genuinely free decisions in the game.

`,md+="The second goalkeeper is the clearest example. He plays roughly never outside a Bench Boost, and every tenth of a million spent on him is a tenth not spent on a starter. ",md+=`The same logic applies to the fifth defender and the third forward: unless you intend to rotate them, they are budget storage rather than assets.

`,md+="The three-per-club limit binds more often than managers expect, particularly when one team's fixtures turn. ",md+=`If a team has four players worth owning, you must choose, and the right choice is usually the one with the most secure minutes rather than the highest ceiling.

`,md+=`## Structuring the squad

`,md+=`- **One premium, budget spread elsewhere.** Frees roughly \xA35m across two mid-price slots. Works when the mid bracket is deep, which the table above will tell you.
`,md+=`- **Two premiums.** Forces genuine \xA34.0m players into defence. Viable only when the cheap defenders are playing ninety minutes rather than sitting on benches.
`,md+=`- **No premium.** Rarely correct, because the most-owned premium sets the score the whole field is measured against, and not owning him is a bet you take every single week rather than once.

`,md+=sdActions([chipRows.length?{verdict:"watch",subject:"Chip timing",reason:`${(chipTotals[chipRows[0][0]]||0).toLocaleString()} managers have already used their ${SD_CHIP_NAME[chipRows[0][0]]||chipRows[0][0]}. Being late is usually better than being with the crowd.`}:null,byPos.length?{verdict:"buy",subject:`Budget into ${[...byPos].sort((a,b)=>b.ppm-a.ppm)[0].pos}`,reason:"Best points per million of any position right now."}:null,{verdict:"hold",subject:"Your structure",reason:"Reshaping a squad mid-season costs points in hits. Decide the shape once and transfer within it."}]),md+=`> With ${rounds} gameweek${rounds===1?"":"s"} played, the points-per-million figures are an early read rather than a settled one. `,md+=`The chip counts and the chip calendar are not estimates \u2014 those are published figures and hold regardless of how much football has been played.
`,{id:"tactical-playbook",title:"The Tactical Playbook: chip timing, budget structure, and where the value sits",category:"Tactical",icon:"",dek:`${chipRows.length?`${(chipTotals[chipRows[0][0]]||0).toLocaleString()} managers have already played a ${SD_CHIP_NAME[chipRows[0][0]]||chipRows[0][0]}. `:""}The structural decisions \u2014 chips and budget distribution \u2014 decide more rank than the weekly transfer does.`,body:md,source:"Generated from published chip usage and the current price and points distribution"}}function sdRoastPick(seed,options){let h=2166136261;const s=String(seed);for(let i=0;i<s.length;i++)h^=s.charCodeAt(i),h=Math.imul(h,16777619);return options[Math.abs(h)%options.length]}function sdRoastAward(icon,title,body){return{icon,title,body}}function sdGenGameweekRoast(){const gw=sdCompletedRound();if(!gw)return null;const rows=sdRoundRows(gw);if(rows.length<20)return null;const ev=sdEvent(gw),played=rows.filter(r=>r.gwMinutes>0);if(played.length<10)return null;const money=r=>`\xA3${r.price.toFixed(1)}m`,pl=(n,one,many)=>`${n} ${n===1?one:many}`,awards=[],blanks=played.filter(r=>r.gwMinutes>=60&&r.gwPoints<=2&&r.price>=6).sort((a,b)=>b.price-a.price||b.own-a.own);if(blanks.length){const b=blanks[0];awards.push(sdRoastAward("","The Golden Blank",sdRoastPick(`blank-${gw}`,[`**${b.name}**, ${money(b)}, ${b.gwMinutes} minutes, ${pl(b.gwPoints,"point","points")}. He was owned by ${b.own}% of the game going in, so at least the disappointment was catered.`,`**${b.name}** cost ${money(b)} and returned ${pl(b.gwPoints,"point","points")} from ${b.gwMinutes} minutes ${b.gwHome?"at home to":"away at"} ${b.gwOpponent}. ${b.own}% of managers watched all of it.`,`${money(b)} buys a lot of things. On this occasion it bought ${b.gwMinutes} minutes of **${b.name}** and ${pl(b.gwPoints,"point","points")}, which ${b.own}% of you had budgeted for rather differently.`])))}const capt=ev&&ev.most_captained?rows.find(r=>r.id===ev.most_captained):null;if(capt){const doubled=capt.gwPoints*2;awards.push(sdRoastAward("","The Armband Tax",capt.gwPoints<=3?sdRoastPick(`capt-bad-${gw}`,[`The game captained **${capt.name}**. He returned ${pl(capt.gwPoints,"point","points")}, which doubled to ${doubled}. Doubling a small number remains one of the great disappointments available to a person on a Saturday.`,`Most-captained: **${capt.name}**. Points: ${capt.gwPoints}. Doubled: ${doubled}. The armband is the biggest call you make each week, and this week most of us made it in unison and were wrong together.`,`**${capt.name}** wore the majority armband and came back with ${pl(capt.gwPoints,"point","points")}. Twice ${capt.gwPoints} is ${doubled}. There is no way to write that sentence that makes it better.`]):sdRoastPick(`capt-ok-${gw}`,[`The field captained **${capt.name}** and, for once, the field was right: ${capt.gwPoints} became ${doubled}. Enjoy it. This award is not usually a compliment.`,`**${capt.name}** was the popular armband and returned ${pl(capt.gwPoints,"point","points")}, which the armband turned into ${doubled}. A rare week where the crowd and the scoreboard agreed.`])))}const diffs=played.filter(r=>r.own<5&&r.gwPoints>=8).sort((a,b)=>b.gwPoints-a.gwPoints||a.own-b.own);if(diffs.length){const d=diffs[0];awards.push(sdRoastAward("","The One You Did Not Own",sdRoastPick(`diff-${gw}`,[`**${d.name}** (${money(d)}) scored ${d.gwPoints} while owned by ${d.own}% of managers. The other ${(100-d.own).toFixed(1)}% are currently explaining to themselves that they would never have started him, which is true, and beside the point.`,`${d.gwPoints} points from **${d.name}**, owned by ${d.own}%. Somewhere a very small number of people are having their best Saturday of the season and telling absolutely everybody.`,`**${d.name}** at ${d.own}% ownership returned ${d.gwPoints}. That is the differential working exactly as advertised, for almost nobody.`])))}const cameos=played.filter(r=>r.gwMinutes>0&&r.gwMinutes<=20).sort((a,b)=>b.gwPoints-a.gwPoints||a.gwMinutes-b.gwMinutes),cameo=cameos.find(r=>r.gwPoints>=3)||cameos[0];cameo&&awards.push(sdRoastAward("","Cameo of the Week",cameo.gwPoints>=3?sdRoastPick(`cameo-good-${gw}`,[`**${cameo.name}** was on the pitch for ${cameo.gwMinutes} minutes and came off it with ${pl(cameo.gwPoints,"point","points")}. Efficiency of that order should be studied, or at least owned.`,`${cameo.gwMinutes} minutes. ${pl(cameo.gwPoints,"point","points")}. **${cameo.name}** did more after coming on than several people did all afternoon, most of them in your starting eleven.`]):sdRoastPick(`cameo-flat-${gw}`,[`**${cameo.name}** managed ${cameo.gwMinutes} minutes for ${pl(cameo.gwPoints,"point","points")}. A brief, complete career.`,`${cameo.gwMinutes} minutes of **${cameo.name}**, ${pl(cameo.gwPoints,"point","points")}. Blink and you would have missed it, which several of his owners did, deliberately.`])));const value=played.filter(r=>r.gwPoints>=6&&r.price>=4).map(r=>({...r,ppm:r.gwPoints/r.price})).sort((a,b)=>b.ppm-a.ppm);if(value.length){const v=value[0];awards.push(sdRoastAward("","Best Value on the Board",sdRoastPick(`value-${gw}`,[`**${v.name}** at ${money(v)} returned ${v.gwPoints} \u2014 ${v.ppm.toFixed(1)} points per million. Premium ownership is a lifestyle choice and this is the invoice.`,`${v.gwPoints} points for ${money(v)}. **${v.name}** did in one afternoon what some of your budget lines have not managed all season.`,`**${v.name}**, ${money(v)}, ${v.gwPoints} points, ${v.ppm.toFixed(1)} per million. Quietly the most sensible thing that happened all weekend.`])))}const robbed=played.filter(r=>r.gwXGI>=.8&&r.gwGoals===0&&r.gwAssists===0).sort((a,b)=>b.gwXGI-a.gwXGI),larceny=played.filter(r=>r.gwGoals+r.gwAssists>=2&&r.gwXGI<=.4).sort((a,b)=>b.gwGoals+b.gwAssists-(a.gwGoals+a.gwAssists));if(robbed.length){const r=robbed[0];awards.push(sdRoastAward("","Mugged by the Woodwork",sdRoastPick(`robbed-${gw}`,[`**${r.name}** generated ${r.gwXGI.toFixed(2)} expected goal involvements and converted precisely none of them. The underlying numbers liked him a great deal more than the scoreboard did, which is either a buy signal or a warning depending on how your season is going.`,`${r.gwXGI.toFixed(2)} xGI, zero returns: **${r.name}** did everything but the last bit. Historically the last bit arrives. Historically it arrives the week after you sell him.`])))}else if(larceny.length){const r=larceny[0];awards.push(sdRoastAward("","Daylight Robbery",sdRoastPick(`larceny-${gw}`,[`**${r.name}** produced ${pl(r.gwGoals+r.gwAssists,"return","returns")} from ${r.gwXGI.toFixed(2)} expected goal involvements. Beautiful to watch, impossible to repeat, and the model would like a word.`,`${pl(r.gwGoals+r.gwAssists,"return","returns")} from ${r.gwXGI.toFixed(2)} xGI. **${r.name}** has been sent from the future to punish everyone who reads underlying statistics.`])))}if(awards.length<3)return null;let md=`## The awards nobody wants

${sdRoastPick(`open-${gw}`,[`Every gameweek produces a handful of moments that are only funny once they cannot hurt you any more. Gameweek ${gw} has finished, so here they are.`,`The serious write-up of Gameweek ${gw} is elsewhere on this desk. This is the other one: the same numbers, read for entertainment rather than instruction.`,`Gameweek ${gw} is in the books, the damage is priced in, and it is now safe to laugh about it.`])}

`;if(md+=`Every award below is a real lookup against the round's data. Nothing here is invented, which is the part that stings.

`,md+=sdTakeaways([blanks.length?`**${blanks[0].name}** takes the Golden Blank at ${money(blanks[0])}.`:null,capt?`The field captained **${capt.name}** for ${pl(capt.gwPoints,"point","points")}.`:null,diffs.length?`**${diffs[0].name}** scored ${diffs[0].gwPoints} at ${diffs[0].own}% ownership.`:null]),awards.forEach(a=>{md+=`### ${a.icon} ${a.title}

${a.body}

`}),ev&&ev.average_entry_score){const beat=ev.highest_score&&ev.average_entry_score?ev.highest_score-ev.average_entry_score:null;md+=`## For the record

`,md+=`The average manager scored **${ev.average_entry_score}**`,md+=ev.highest_score?`, the best score in the game was **${ev.highest_score}**`:"",md+=beat?`, and the gap between them was ${beat} points \u2014 roughly a captain, a bench and a small amount of luck.`:".",md+=`

`}const closer=sdRoastPick(`close-${gw}`,["None of this is advice. The advice is next door, and it is considerably better behaved.","The useful version of this gameweek is in the Debrief. This one was for morale.",`Normal analytical service resumes on the rest of the desk. Gameweek ${gw+1} will produce its own list, and one of these entries will probably be yours.`]);md+=sdCallout("insight",closer);const headline=blanks.length?blanks[0]:capt||played[0];return{subject:headline?sdSubject(headline,`${headline.gwPoints} pts \xB7 \xA3${(headline.price||0).toFixed(1)}m`):null,id:`gw-${gw}-hall-of-shame`,title:`Gameweek ${gw} Hall of Shame: the blanks, the cameos, and the captain who cost you`,category:"Hall of Shame",icon:"",dek:`The awards nobody wants from Gameweek ${gw}${headline?`, starting with ${headline.name}`:""}. Every one of them a real number, which is the problem.`,body:md,source:"Official FPL data"}}const SD_RECURRING=[{key:"gw-debrief",label:"The Gameweek Debrief",cadence:"Weekly",gen:sdGenGameweekDebrief},{key:"hall-of-shame",label:"The Hall of Shame",cadence:"Weekly",gen:sdGenGameweekRoast},{key:"pre-deadline-captaincy",label:"Captaincy Matrix",cadence:"Weekly",gen:sdGenPreDeadlineCaptaincy},{key:"under-the-hood",label:"Under the Hood",cadence:"Every 2 weeks",gen:sdGenUnderTheHood},{key:"fixture-horizon",label:"The Fixture Horizon",cadence:"Every 2 weeks",gen:sdGenFixtureHorizon},{key:"market-digest",label:"Market Watch",cadence:"Every 2 weeks",gen:sdGenMarketDigest},{key:"tactical-playbook",label:"The Tactical Playbook",cadence:"Monthly",gen:sdGenTacticalPlaybook}];let _sdFaceIndex=null,_sdFaceSource=null;function sdFaceIndex(){return _sdFaceIndex&&_sdFaceSource===sdBoot||(_sdFaceSource=sdBoot,_sdFaceIndex=new Map,(sdBoot?.elements||[]).forEach(e=>{if(e.code==null)return;const key=String(e.web_name||"").toLowerCase();key&&!_sdFaceIndex.has(key)&&_sdFaceIndex.set(key,e.code)})),_sdFaceIndex}function sdFaceFor(cellHtml){const text=String(cellHtml).replace(/<[^>]*>/g,"").trim();if(!text||text.length>30)return null;const code=sdFaceIndex().get(text.toLowerCase());return code??null}function sdFaceCell(cellHtml){const code=sdFaceFor(cellHtml);if(code==null)return cellHtml;const base="https://resources.premierleague.com",alts=[`${base}/premierleague25/photos/players/110x140/${code}.png`,`${base}/premierleague/photos/players/250x250/p${code}.png`,`${base}/premierleague/photos/players/110x140/p${code}.png`];return`<span class="sd-face-cell"><img class="sd-face" alt="" loading="lazy" decoding="async" src="${base}/premierleague25/photos/players/250x250/${code}.png" data-photo-alts="${alts.join(" ")}" onerror="var a=(this.dataset.photoAlts||'').split(' ').filter(Boolean); if (a.length) { this.src = a.shift(); this.dataset.photoAlts = a.join(' '); } else { this.remove(); }"><span>${cellHtml}</span></span>`}function sdMarkdown(md){const esc=s=>s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;"),fdrPill=s=>s.replace(/\{fdr:([\d.]+)\}/g,(_,n)=>{const v=parseFloat(n)||0;return`<span class="fdr-pill fdr-${Math.max(1,Math.min(5,Math.round(v)))}">${v.toFixed(1)}</span>`}),inline=s=>fdrPill(esc(s).replace(/\*\*(.+?)\*\*/g,"<strong>$1</strong>").replace(/`(.+?)`/g,"<code>$1</code>")),ACTION={buy:{cls:"action-buy",dot:"",word:"BUY"},hold:{cls:"action-hold",dot:"",word:"HOLD"},sell:{cls:"action-sell",dot:"",word:"SELL"},start:{cls:"action-buy",dot:"",word:"START"},bench:{cls:"action-sell",dot:"",word:"BENCH"},watch:{cls:"action-hold",dot:"",word:"WATCH"}};function directive(kind,variant,body){if(kind==="callout"){const label=SD_CALLOUT_LABEL[variant]?variant:"insight",paras=body.join(`
`).split(/\n\s*\n/).filter(t=>t.trim());return`<aside class="scout-callout scout-callout-${label}"><div class="callout-header">${inline(SD_CALLOUT_LABEL[label])}</div>`+paras.map(t=>`<p>${inline(t.replace(/\n/g," ").trim())}</p>`).join("")+"</aside>"}if(kind==="actions"){const cards=body.map(line=>{const[verdictRaw,subject,...rest]=line.split("|"),a=ACTION[(verdictRaw||"").trim().toLowerCase()];return!a||!subject?"":`<div class="action-card ${a.cls}"><span class="action-dot">${a.dot}</span><div><strong>${a.word}:</strong> ${inline(subject.trim())}`+(rest.length?`<span class="action-why">${inline(rest.join("|").trim())}</span>`:"")+"</div></div>"}).join("");return cards?`<div class="article-action-grid">${cards}</div>`:""}if(kind==="takeaways"){const items=body.filter(l=>/^-\s/.test(l)).map(l=>`<li>${inline(l.replace(/^-\s/,""))}</li>`).join("");return items?`<aside class="article-takeaways"><div class="callout-header">The short version</div><ul>${items}</ul></aside>`:""}if(kind==="fixtures"){const cells=body.map(line=>{const[gw,opp,diff]=line.split("|").map(s=>(s||"").trim());return!gw||!opp?"":`<span class="fixture-chip-strip fdr-${Math.max(1,Math.min(5,Math.round(parseFloat(diff)||3)))}"><small>${inline(gw)}</small>${inline(opp)}</span>`}).join("");return cells?`<div class="article-fixture-strip">${cells}</div>`:""}return""}const lines=md.split(`
`);let html="",i=0,open=!1,blockN=0,blockClassAt=-1,blockContentAt=-1;const closeBlock=()=>{if(!open)return;const inner=html.slice(blockContentAt),mods=[];blockN===1&&mods.push("sd-block--lead"),/class="sd-table-wrap/.test(inner)&&mods.push("sd-block--table"),/^\s*<aside class="scout-callout/.test(inner)&&!/<p[ >]/.test(inner)&&mods.push("sd-block--callout"),mods.length&&(html=html.slice(0,blockClassAt)+" "+mods.join(" ")+html.slice(blockClassAt)),html+="</section>",open=!1},openBlock=heading=>{closeBlock(),blockN++;const tag='<section class="sd-block';blockClassAt=html.length+tag.length,html+=`${tag}"><h2 class="sd-block-h">${heading}</h2>`,blockContentAt=html.length,open=!0};for(;i<lines.length;){const line=lines[i];if(/^\s*$/.test(line)){i++;continue}if(/^:::/.test(line)){const m=/^:::\s*([a-z]+)(?:\s+([a-z]+))?/i.exec(line);i++;const inner=[];for(;i<lines.length&&!/^:::\s*$/.test(lines[i]);)inner.push(lines[i]),i++;i++,m&&(html+=directive(m[1].toLowerCase(),(m[2]||"").toLowerCase(),inner));continue}if(/^###\s/.test(line)){html+=`<h3>${inline(line.replace(/^###\s/,""))}</h3>`,i++;continue}if(/^##\s/.test(line)){openBlock(inline(line.replace(/^##\s/,""))),i++;continue}if(/^>\s?/.test(line)){let quote=[];for(;i<lines.length&&/^>\s?/.test(lines[i]);)quote.push(lines[i].replace(/^>\s?/,"")),i++;html+=`<blockquote>${inline(quote.join(" "))}</blockquote>`;continue}if(/^\|/.test(line)&&i+1<lines.length&&/^\|[\s:|-]+\|$/.test(lines[i+1])){const cells=r=>r.split("|").slice(1,-1).map(c=>inline(c.trim())),head=cells(line);i+=2;const body=[];for(;i<lines.length&&/^\|/.test(lines[i]);)body.push(cells(lines[i])),i++;html+=`<div class="sd-table-wrap article-table-wrapper"><table class="sd-table article-data-table"><thead><tr>${head.map(h=>`<th>${h}</th>`).join("")}</tr></thead><tbody>${body.map(r=>`<tr>${r.map(c=>`<td>${sdFaceCell(c)}</td>`).join("")}</tr>`).join("")}</tbody></table></div>`;continue}if(/^[-*]\s/.test(line)){const items=[];for(;i<lines.length&&/^[-*]\s/.test(lines[i]);)items.push(inline(lines[i].replace(/^[-*]\s/,""))),i++;html+=`<ul>${items.map(it=>`<li>${it}</li>`).join("")}</ul>`;continue}const para=[];for(;i<lines.length&&!/^\s*$/.test(lines[i])&&!/^[#>|]|^[-*]\s|^:::/.test(lines[i]);)para.push(lines[i]),i++;para.length&&(html+=`<p>${inline(para.join(" "))}</p>`)}return closeBlock(),html}function sdFormatDate(d){return d.toLocaleDateString(void 0,{day:"numeric",month:"short",year:"numeric"})}function renderArticlesPage(){const el=document.getElementById("sdFeed");if(!el)return;if(!sdArticles.length){el.innerHTML='<div class="sd-empty">No articles could be generated \u2014 the FPL data has not loaded.</div>';return}const byGw=new Map;sdArticles.forEach(a=>{const key=a.gw==null?"evergreen":a.gw;byGw.has(key)||byGw.set(key,[]),byGw.get(key).push(a)});const rounds=[...byGw.keys()].filter(k=>k!=="evergreen").sort((a,b)=>b-a),card=a=>`
        <a class="sd-card" href="${sdPermalink(a)}" onclick="return sdCardClick(event, '${a.id}')">
            ${sdArtwork(a,"card")}
            <div class="sd-card-body">
                <div class="sd-card-top">
                    <span class="sd-tag">${escHTML(a.category)}</span>
                    <span class="sd-read">${a.readTime} min</span>
                </div>
                <h3 class="sd-card-title">${escHTML(a.title)}</h3>
                <p class="sd-card-dek">${escHTML(a.dek)}</p>
                <div class="sd-meta">${sdFormatDate(a.date)}</div>
            </div>
        </a>`,lead=a=>`
        <a class="sd-featured" href="${sdPermalink(a)}" onclick="return sdCardClick(event, '${a.id}')">
            ${sdArtwork(a,"lead")}
            <div class="sd-featured-body">
                <div class="sd-tags">
                    <span class="sd-tag primary">${escHTML(a.category)}</span>
                    <span class="sd-read">${a.readTime} min read</span>
                </div>
                <h2 class="sd-featured-title">${escHTML(a.title)}</h2>
                <p class="sd-featured-dek">${escHTML(a.dek)}</p>
                <div class="sd-meta">${sdFormatDate(a.date)} \xB7 ${escHTML(a.source)}</div>
            </div>
        </a>`,section=(label,note,items,isLead)=>{if(!items.length)return"";const[first,...rest]=items;return`<section class="sd-round${isLead?" is-lead":""}">
            <div class="sd-round-head">
                <h2 class="sd-round-title">${escHTML(label)}</h2>
                
                ${note?`<span class="sd-round-note">${escHTML(note)}</span>`:""}
            </div>
            ${isLead?lead(first):""}
            <div class="sd-grid">${(isLead?rest:items).map(card).join("")}</div>
        </section>`},latest=rounds[0];el.innerHTML=(latest!=null?section(`Gameweek ${latest}`,"The latest round",byGw.get(latest),!0):"")+rounds.slice(1).map(gw=>section(`Gameweek ${gw}`,"",byGw.get(gw),!1)).join("")+section("Always true","Not about any one round",byGw.get("evergreen")||[],!1)}function sdPermalink(a){return a.slug?`articles/${a.slug}.html`:"/dashboard/scouts-desk"}function sdCardClick(event,id){return event.metaKey||event.ctrlKey||event.shiftKey||event.button===1?!0:(event.preventDefault(),sdOpenArticle(id),!1)}async function sdOpenArticle(id){const a=sdArticles.find(x=>x.id===id);if(a){if(!a.body&&a.slug)try{const full=await DataCache.fetchJSON(`data/articles/${a.slug}.json?v=${CACHE_BUSTER}`);a.body=full.body}catch{a.body="_This article could not be loaded._"}sdRenderArticle(a)}}function sdRenderArticle(a){document.getElementById("sdReaderBody").innerHTML=`
        <!-- The same hero the permalink page carries. Opening a piece in the
             reader and opening it at its own URL should not be two different
             articles. -->
        <div class="sd-standalone-art">${sdArtwork(a,"lead")}</div>
        <div class="sd-tags">
            <span class="sd-tag primary">${escHTML(a.category)}</span>
            <span class="sd-read">${a.readTime} min read</span>
        </div>
        <h1 class="sd-reader-title">${escHTML(a.title)}</h1>
        <p class="sd-reader-dek">${escHTML(a.dek)}</p>
        <div class="sd-reader-meta">${sdFormatDate(a.date)} \xB7 ${escHTML(a.source)}</div>
        <div class="sd-prose">${sdMarkdown(a.body)}</div>
        <div class="sd-reader-foot">
            ${a.slug?`Published from the FPL dataset as it stood that day, and unedited since. <a class="sd-permalink" href="articles/${a.slug}.html">Permanent link</a>`:"Generated from the live FPL dataset when this page loaded."}
        </div>`,document.getElementById("sdReader").classList.add("open"),document.body.style.overflow="hidden",document.getElementById("sdReaderScroll").scrollTop=0}function sdCloseArticle(event){event&&event.target instanceof Element&&event.target.closest(".sd-reader-body")||(document.getElementById("sdReader").classList.remove("open"),document.body.style.overflow="")}function sdGenerate(key){const spec=SD_RECURRING.find(r=>r.key===key);if(!spec)return;const art=spec.gen(),status=document.getElementById("sdGenStatus");if(!art){status&&(status.textContent=`Not enough data to generate ${spec.label} yet.`);return}art.words=sdWordCount(art.body),art.readTime=sdReadTime(art.body),art.date=new Date,sdArticles.unshift(art),renderArticlesPage(),status&&(status.textContent=`Published \u201C${art.title}\u201D.`),sdOpenArticle(art.id)}function sdToggleStudio(){const el=document.getElementById("sdStudio");el&&el.classList.toggle("open")}async function initScoutsDesk(){try{const[boot,fixtures,players]=await Promise.all([DataCache.fetchJSON(DATA_URLS.bootstrap),DataCache.fetchJSON(DATA_URLS.fixtures).catch(()=>[]),DataCache.fetchJSON(DATA_URLS.players).catch(()=>null)]);sdBoot=boot,sdFixtures=fixtures,sdHistory=players;let archive=null;try{archive=await DataCache.fetchJSON(`data/articles/index.json?v=${CACHE_BUSTER}`)}catch{}if(archive&&archive.length)sdArticles=archive.map(a=>({...a,id:a.slug,date:new Date(a.date)})),sdArchived=!0;else{const built=[sdGenGameweekDebrief(),sdGenGameweekRoast(),sdGenPreDeadlineCaptaincy(),sdGenUnderTheHood(),sdGenFixtureHorizon(),sdGenMarketDigest(),sdGenTacticalPlaybook(),sdGenInsideAlgorithm()].filter(Boolean),now=Date.now();sdArticles=built.map((a,i)=>({...a,readTime:sdReadTime(a.body),date:new Date(now-i*864e5*2)})),sdArchived=!1}document.getElementById("sdStudioList").innerHTML=SD_RECURRING.map(r=>`<button class="sd-gen-btn" onclick="sdGenerate('${r.key}')">
                <span class="sd-gen-label">${escHTML(r.label)}</span>
                <span class="sd-gen-cadence">${escHTML(r.cadence)}</span>
            </button>`).join(""),renderArticlesPage();const rounds=sdRoundsPlayed(),note=document.getElementById("sdDataNote");if(note){const sample=rounds<=2?"Early-season samples are small, and the articles say so where it matters.":`Drawn from ${rounds} completed gameweeks of FPL data.`;note.textContent=sdArchived?`${sdArticles.length} articles in the archive, each published once and kept as it was. ${sample}`:`Generated live from the current FPL data \u2014 the archive has not been built yet. ${sample}`}}catch(e){console.error("Scout's Desk failed to load:",e);const el=document.getElementById("sdFeed");el&&(el.innerHTML='<div class="sd-empty">Could not load the FPL data behind these articles. Try again shortly.</div>')}}typeof document<"u"&&document.addEventListener("keydown",e=>{e.key==="Escape"&&sdCloseArticle()});function sdSetData(boot,fixtures,history){sdBoot=boot,sdFixtures=fixtures,sdHistory=history,sdBuildProjection()}let sdXP=null;function sdBuildProjection(){if(sdXP=null,!(!sdBoot||!sdBoot.teams||!sdBoot.elements))try{const engine=sdLoadXpEngine();if(!engine)return;const teamsById={};sdBoot.teams.forEach(t=>{teamsById[t.id]=t}),engine.teamFixtures6=engine.xpBuildTeamFixtures(sdFixtures,teamsById,8),engine.teamAnalysis=engine.xpBuildTeamScores(sdBoot.teams,sdFixtures),engine.seasonStats=engine.xpBuildSeasonStats(sdBoot.teams,sdFixtures),engine.allPlayers=engine.xpBuildPlayers(sdBoot.elements,teamsById,engine.teamFixtures6),engine.positionAverages=engine.xpBuildPositionAverages(engine.allPlayers,sdNextGw());const byId={};engine.allPlayers.forEach(pl=>{byId[pl.id]=pl}),sdXP={engine,byId}}catch{sdXP=null}}function sdLoadXpEngine(){if(typeof require>"u")return null;let src;try{const fs=require("fs"),path=require("path");src=fs.readFileSync(path.join(__dirname,"xp-engine.js"),"utf8")}catch{return null}const ctx={isPreseason:!1,currentGW:sdNextGw()},names=["xpBuildTeamFixtures","xpBuildTeamScores","xpBuildSeasonStats","xpBuildPlayers","xpBuildPositionAverages","projectPlayerPointsForGW","predictedGWPoints","expectedMinutesModel"];return new Function("__ctx",`with (__ctx) {
${src}
${names.map(n=>`__ctx.${n} = typeof ${n} === 'function' ? ${n} : null;`).join(`
`)}
}`)(ctx),ctx.projectPlayerPointsForGW?ctx:null}function sdProjectedPoints(player,gw){if(sdXP&&sdXP.byId[player.id])try{const xp=sdXP.engine.projectPlayerPointsForGW(sdXP.byId[player.id],gw);if(Number.isFinite(xp))return{xp,modelled:!0}}catch{}const rounds=Math.max(1,sdRoundsPlayed());return{xp:Math.round(player.points/rounds*10)/10,modelled:!1}}function sdSlugify(s){return String(s).toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")}function sdRecurringDue(gw){const due=[];return gw%2===1&&due.push({key:"under-the-hood",gen:sdGenUnderTheHood}),gw%2===0&&due.push({key:"fixture-horizon",gen:sdGenFixtureHorizon}),gw%2===1&&due.push({key:"market-digest",gen:sdGenMarketDigest}),gw%4===2&&due.push({key:"tactical-playbook",gen:sdGenTacticalPlaybook}),due}function sdBuildArchive(){const gw=sdLastRound(),done=sdCompletedRound(),out=[],push=(art,slug,dated)=>{art&&(art.slug=slug,art.words=sdWordCount(art.body),art.readTime=sdReadTime(art.body),art.gw=dated??null,out.push(art))};return gw>0&&(done>0&&push(sdGenGameweekDebrief(),`gameweek-${done}-debrief`,done),done>0&&push(sdGenGameweekRoast(),`gameweek-${done}-hall-of-shame`,done),push(sdGenPreDeadlineCaptaincy(),`gameweek-${sdNextGw()}-captaincy-matrix`,sdNextGw()),sdRecurringDue(gw).forEach(r=>push(r.gen(),`gameweek-${gw}-${r.key}`,gw))),push(sdGenInsideAlgorithm(),"inside-the-algorithm",null),out}typeof module<"u"&&module.exports&&(module.exports={sdSetData,sdBuildArchive,sdMarkdown,sdReadTime,sdWordCount,sdLastRound,sdRoundsPlayed,sdSlugify,sdRecurringDue,sdNextGw,sdRoastPick,sdGenGameweekRoast,sdProjectedPoints});
