User-agent: *
Allow: /

# /dashboard is index.html served in place (see _redirects), so indexing it
# would file the same document under two URLs. The landing page at / is the
# one that should rank, and nothing behind /dashboard is meaningful to a
# crawler anyway — it has no saved team, so it renders the landing page.
Disallow: /dashboard
Sitemap: https://easyfpl.com/sitemap.xml
